% =========================================================================
% How Much of a Convolutional Layer's Width Is Actually Used?
%
% Sections I-III are drafted.  Sections IV-VI wait on data.
%
% \num{...} marks a figure taken from a secondary source that must be checked
% against the original before submission.  \todo{...} marks unwritten text.
%
% The analysis plan lives at ../notes/analysis-plan.md.  If this document and
% that one disagree, the plan wins and this file is out of date.
% Verification notes for three contested references: ../refs/verification-notes.md
%
% Build:  make          Outstanding items:  make todo
% =========================================================================

\documentclass[10pt,journal,compsoc]{IEEEtran}

\usepackage{amsmath,amssymb,amsthm}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{url}
\usepackage[caption=false,font=footnotesize]{subfig}
\usepackage[hidelinks]{hyperref}

\graphicspath{{../results/figures/}}

% --- drafting aids; delete before submission ------------------------------
\usepackage{xcolor}
\newcommand{\todo}[1]{\textcolor{red}{[\textbf{TODO} #1]}}
\newcommand{\num}[1]{\textcolor{blue}{#1}}   % secondary-source figure: verify
% -------------------------------------------------------------------------

\begin{document}

\title{Measuring the Effective Width of Convolutional Layers: Protocol, Pitfalls, and the Comparison with Initialisation}

\author{Cheng-Chung~Li%
\IEEEcompsocitemizethanks{\IEEEcompsocthanksitem C.-C. Li is with the Taiwan Power
Research Institute, Taiwan Power Company, Taipei, Taiwan.\protect\\
E-mail: u324057@taipower.com.tw}%
}

\markboth{IEEE Transactions on Pattern Analysis and Machine Intelligence}%
{Li: Measuring the Effective Width of Convolutional Layers}

\IEEEtitleabstractindextext{%
\begin{abstract}
How much of a convolutional layer's channel width does a trained network
use? Two published depth profiles of this quantity disagree. Measuring the
channel covariance of every convolutional layer in 19 public ImageNet
checkpoints, we show that three choices the profile literature makes
silently each move a profile by more than the differences it reports. A
$1\!\times\!1$ expansion layer cannot have output rank above its input width,
a known bound never carried into the denominator: dividing by attainable
rather than nominal width removes an apparent two-population structure in
all 14 models with such layers. Estimating the covariance from globally
pooled features lowers the value in 687 of 703 dense layers. And the output
of a residual block exceeds the bound on its last convolution in 161 of 161
bottleneck blocks, so block-output and convolution-output profiles are
different objects. With these fixed, the protocol recovers the published
CIFAR-10 profile of Garg \emph{et al.} ($r = 0.98$) and the ImageNet block
values of Elmoznino and Bonner ($\rho = 0.997$); truncating every layer to its $k^*(0.999)$ principal directions leaves accuracy unchanged in six of eight checkpoints and costs $1.7$ points in two, while $k^*(0.95)$ does not preserve it. Because the output covariance before the nonlinearity
is exactly $W\Sigma_{\rm patch}W^{\top}$, the statistic separates into a kernel
factor and a data factor, and training moves the bottleneck from the data
to the kernel. Against their own initialisations, trained networks largely
keep the architecture's layer ordering and gain a level, in place before
accuracy is; a pre-registered controlled experiment shows the block type
does not change this, so ImageNet ResNet-50, the one family whose
checkpoints reorder their layers, owes it to its training regime.
Orthogonality penalties realise half of the width gain the decomposition
predicts, in the layers it predicts. Code, spectra and the pre-registered
analysis plan are released.
\end{abstract}

\begin{IEEEkeywords}
Convolutional networks, effective dimensionality, channel covariance,
network width, representation analysis.
\end{IEEEkeywords}}

\maketitle

% =========================================================================
\IEEEdisplaynontitleabstractindextext
\IEEEpeerreviewmaketitle


% =========================================================================
\IEEEraisesectionheading{\section{Introduction}\label{sec:intro}}

\IEEEPARstart{H}{ow} many channels a convolutional layer should have is,
in almost all practice, decided before training by inheritance from a
published architecture or by search, and how many it ends up using is
known only afterwards. Theory bounds the number from above, by the rank a
convolution can attain and by the rank of what it receives, and for
normalised linear networks from below \cite{daneshmand2020bn}; the
minimum-width theorems of approximation theory
\cite{lu2017expressive,park2021minimum} bound a different quantity, the
width a family of networks needs to be dense in a function class. Outside
the lazy regime in which features do not move \cite{jacot2018ntk,lee2020wide},
no estimator predicts the width a trained layer will actually realise:
every such number in the literature is taken after training
\cite{zhang2016accelerating,ansuini2019intrinsic,lin2020hrank}, from a
narrower network that was itself trained \cite{vyas2023feature}, or from
random weights without a guarantee \cite{han2021rexnet}. If the post-hoc
number is to inform design, three things must be settled first: how it is
to be measured so that two papers' values are comparable; what it counts,
in the sense of which directions carry function; and how much of it was
fixed before training by the architecture and the data, as opposed to
learned. This paper is about those three things.

A prior question is descriptive: of the width a trained network \emph{has},
how much does it use? Concretely, how many directions in a layer's channel
space carry its activation variance, as a fraction of the directions
available? The question has been asked twice at scale, with different
answers. Garg \emph{et al.} \cite{garg2019lowEffort} computed, for each
convolutional layer of VGG networks on CIFAR, the number of principal
components of the channel-space activation covariance needed to reach a fixed
fraction of the variance, and reported a profile that saturates in the middle
of the network and collapses in its last layers. Elmoznino and Bonner
\cite{elmoznino2024highDim} computed the participation ratio across
\num{500}\,+ convolutional layers of ImageNet-trained models and reported
effective dimensionality \emph{increasing} with depth.

Setting out to reconcile these, we found that the two measurements differ in
ways that are not about the networks at all, and that the same choices
silently shape any new measurement one might make. Three of them matter.

\emph{The denominator.} A $1\!\times\!1$ convolution that expands $C_{\rm in}$
channels to $C_{\rm out} > C_{\rm in}$ cannot produce an output of rank above
$C_{\rm in}$. The bound is elementary and has been stated for convolutional
expansion layers before \cite{han2021rexnet,kim2018rank1}, but no depth
profile in the measurement literature divides by it. Twenty of ResNet-50's 53 convolutions are such layers, so a profile normalised by nominal width is capped at $0.25$ on 20 of its 53 layers; at the $95\%$ criterion those layers read $0.11$--$0.13$, and the profile says that wide layers use a tenth of their width.

\emph{The estimator.} Garg \emph{et al.} treat spatial positions as samples;
Elmoznino and Bonner average each feature map to one vector per image before
estimating the covariance. The two covariances are different objects, and
they had not been compared on the same layers.

\emph{The measurement point.} Some studies hook the output of a convolution;
others hook the output of a residual block, after the shortcut has been added.
The rank bound above binds the former and not the latter.

This paper does two things. It fixes a measurement protocol in which each of
these choices is made explicitly and its consequence quantified, and it then
uses the protocol to ask what training actually changes about a layer's
effective width. The contributions are:

\begin{itemize}
  \item \textbf{A protocol, validated.} We measure the channel covariance of
  the convolution output (before activation and before any shortcut) from
  sampled spatial positions, at a sample-to-channel ratio of at least $50$,
  normalised by attainable rank, with depthwise and dense convolutions kept
  apart. On a VGG-16 we train ourselves on CIFAR-10 the protocol recovers the
  13-layer profile published by Garg \emph{et al.} \cite{garg2019lowEffort}
  with $r = 0.98$ and a mean absolute difference of $0.05$; two layers, \texttt{features.27} and \texttt{features.30}, differ by $0.2$. On ResNet-50 it agrees with the published block-output participation ratios of Elmoznino and Bonner \cite{elmoznino2024highDim} with $\rho = 0.997$ over 16 blocks (Section~\ref{sec:repro-garg}).

  \item \textbf{Three pitfalls, quantified across 19 checkpoints.} (The estimator comparison was pre-registered; the other two were found in the data.) Normalising
  by attainable rank removes the bounded-versus-unbounded difference in all 14
  models that contain expansion layers, for expansion ratios of 2, 4 and 6.
  The pooled estimator gives a lower value than the position-sampled one in
  687 of 703 dense layers and 79 of 80 depthwise layers, 14 of the 17 exceptions being ties. The output of a bottleneck block exceeds the rank
  bound of its last convolution in 161 of 161 blocks, by a factor of about
  four (Sections~\ref{sec:rmax}, \ref{sec:estimator} and \ref{sec:blockout}).

  \item \textbf{What training changes, compared with initialisation.} With one to six trained checkpoints and three to five random initialisations for each of eleven families, measured on the same images, we compare the ordering of layers in the trained profile with the ordering at initialisation. In plain,
  basic-residual and densely connected networks (VGG-16, ResNet-18/34,
  DenseNet-121) the ordering is largely architectural
  ($\rho_{\rm trained,random} = 0.6$--$0.75$) and training raises the level by $0.09$--$0.19$ in the median over layers, more than the profile's own adjacent-layer step in three of the four families. A controlled experiment registered before its data were seen, basic-block and bottleneck ResNets of equal depth, width pattern and recipe on CIFAR-10 with three seeds each, finds the same for both block types ($\rho = 0.55$ and $0.50$), and nine training trajectories find the ordering in place by epoch 40--60 while accuracy is still at $84$--$88\%$. The exception among public checkpoints is ImageNet ResNet-50, whose six recipes agree with each other ($\rho = 0.84$) and with initialisation not at all ($-0.08$); the controlled experiment says this is not the bottleneck block's doing but the training regime's, which the experiment does not identify. The inverted-residual checkpoints one can download (MobileNetV3, EfficientNet-B0) do not share a profile and their $1\!\times\!1$ levels vary four- to seven-fold, but three seeds of one recipe agree at $\rho \ge 0.99$, so that disagreement is the recipes'
  (Section~\ref{sec:learned}).

  \item \textbf{An exact decomposition of the statistic.} Before the nonlinearity the output covariance is $W\Sigma_{\rm patch}W^{\top}$, so a layer's effective width separates into a kernel factor and a patch-covariance factor. Measuring both on ten checkpoints shows that training moves the bottleneck from the data to the kernel: at initialisation the patch covariance is low-rank and the kernel isotropic; after training the patch covariance is full-rank and the ordering of layers follows the kernels' Gram spectra ($\rho = 0.8$--$0.97$ in six of nine trained checkpoints). Orthogonalising a trained kernel with its input held fixed would raise its effective width by about $0.3$ of the attainable rank. Tested as registered, with soft-orthogonality and SRIP penalties on CIFAR-10, the penalties realise half of that gain, and the soft penalty realises it in the layers the decomposition predicts (Section~\ref{sec:decompose}).

  \item \textbf{Three further observations.} Truncating every convolution output to its top $k^*(\tau)$ principal channel directions at inference, the response-PCA operation of Zhang \emph{et al.} \cite{zhang2016accelerating} applied with the measured rank at every layer at once and without fine-tuning, leaves top-1 accuracy unchanged at $\tau = 0.999$ (McNemar) in six of eight checkpoints, including four ResNet-50 recipes, ResNet-18 and VGG-16, while the two ResNet-50 recipes of Wightman \emph{et al.} lose $1.7$ points; at the pre-registered $\tau = 0.95$ the cost ranges from $19$ points to near-total collapse depending on the recipe, and random directions of the same dimension give chance throughout. This calibrates what the statistic counts; it is not a compression result (Section~\ref{sec:projection}). Along one CIFAR-10 training run of VGG-16 the profile is in place by epoch 40, while test accuracy still rises from $84\%$ to $94\%$. And the position-sampled statistic
  computed from $6{,}400$ images agrees with the $50{,}000$-image value to
  within $0.016$ on every layer of six checkpoints, which bounds how much data such a measurement needs (Sections~\ref{sec:samplesize} and \ref{sec:learned}).
\end{itemize}

What the reader can take away is a ruler and three facts. The ruler is a protocol under which effective-width profiles from different papers are on one scale, validated against both published estimators at both tensors; it is released as code and can be run on any convolutional network in minutes. The facts are that the profile literature's disagreement is largely about tensors, denominators and tasks rather than about networks; that bottleneck networks have been misread as underused because of a denominator; and that what a trained layer uses is set jointly by its kernel and its input, with training moving the constraint from the second to the first, which gives orthogonality regularisation a quantified, testable prediction rather than an assumption.

The analysis was specified before the ImageNet data were collected. The pre-registered hypotheses concerned the \emph{shape} of the profile, and none of them survived a paired test across all layers. The estimator comparison, the statistic comparison, the threshold sensitivity and the sample-size control were pre-registered; the attainable-rank denominator, the block-output comparison, the depthwise split and the block-type comparison were arrived at after the data were seen, and every result of the second kind is labelled exploratory where it appears. The plan, and a log of every departure from it, is released with the
code (Section~\ref{sec:repro}).
% =========================================================================
\section{Related Work}
\label{sec:related}

\subsection{Four notions of ``dimensionality''}
\label{sec:four}

At least four distinct quantities are described in this literature as the
dimensionality of a representation, and they are not interchangeable. The
first is the intrinsic dimension of the data manifold, estimated by nonlinear
methods such as the two-nearest-neighbour estimator of Ansuini \emph{et al.}
\cite{ansuini2019intrinsic}. The second is the rank of the linear span of the
representation. The third is the participation ratio of the covariance
spectrum. The fourth is the number of principal components required to reach a
chosen fraction of the total variance.

The gap between the first and the rest is not a matter of degree. For the same
hidden layer of the same VGG-16 network, Ansuini \emph{et al.} report a
manifold dimension of approximately \num{18} against approximately \num{200}
principal components at a $90\%$ variance criterion---one to two orders of
magnitude apart. They state explicitly that their findings cannot be recovered
by linear estimates such as PCA.

Everything measured in this paper is linear: every quantity we report is a function of the eigenvalues of a channel-space covariance matrix. We make no claim about manifold dimension, and where prior results are cited we name the quantity rather than the word. The linear count is nonetheless the right object for the question of width, for a reason that does not depend on the geometry of the representation: the consumer of a layer's output is the next convolution, which is linear in its input patch, and what a linear map can use of its input is exactly the linear span of that input. A layer whose output covariance has rank $k$ presents $k$ directions to the next kernel whatever the curvature of the manifold those directions carry, so the number of channels the next layer needs is a statement about the linear span, and the manifold dimension is a statement about what the network has encoded within it. Section~\ref{sec:projection} makes the first statement operational: the directions beyond the counted ones can be removed at inference without loss.

\subsection{Depth profiles of representation dimensionality}
\label{sec:profiles}

Several groups have reported how a dimensionality measure varies with depth; Table~\ref{tab:survey} lists them by what they read, how they sample, what they compute and against what they normalise, which is the comparison this paper is about. The reported shape depends on the measure. Ansuini \emph{et al.}
\cite{ansuini2019intrinsic} found that manifold intrinsic dimension rises in
early layers, peaks at a relative depth of roughly \num{0.2--0.4}, and then
decreases monotonically---a shape that Recanatesi \emph{et al.}
\cite{recanatesi2019dimensionality} also observed, and that Rangamani
\emph{et al.} \cite{rangamani2023intermediate} recovered again using stable
rank on intermediate layers.

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\caption{Per-layer dimensionality profiles of CNN representations in prior work.
Tensor: what is read at each layer; Samples: what plays the role of a sample;
Denom.: normaliser applied to the statistic; Untr.: untrained control; Dyn.:
training dynamics; Shape: reported profile along depth. ``Lin.\ rank'' groups
numerical/stable/effective rank of the activation (or centred activation)
matrix; ``ID'' is a manifold intrinsic-dimension estimator.}
\label{tab:survey}
\begin{tabular}{@{}>{\raggedright\arraybackslash}p{2.4cm}>{\raggedright\arraybackslash}p{3.0cm}>{\raggedright\arraybackslash}p{2.3cm}>{\raggedright\arraybackslash}p{1.7cm}>{\raggedright\arraybackslash}p{2.1cm}>{\raggedright\arraybackslash}p{1.1cm}cc>{\raggedright\arraybackslash}p{2.2cm}@{}}
\toprule
Work & Networks / data & Tensor & Samples & Statistic & Denom. & Untr. & Dyn. & Shape \\
\midrule
\multicolumn{9}{@{}l}{\emph{Manifold intrinsic dimension}} \\
Ansuini et al. \cite{ansuini2019intrinsic} & AlexNet, VGG, ResNet; ImageNet & block / pool out & images (7$\times$500) & TwoNN ID, PC-ID & -- & \checkmark & (\checkmark) & hunchback \\
Recanatesi et al. \cite{recanatesi2019dimensionality} & MLP, ResNet; F-MNIST, CIFAR & layer out & images & local/global ID & -- & \checkmark & before/after & expand--compress \\
Cohen et al. \cite{cohen2020manifolds} & AlexNet, VGG-16, ResNet-50; ImageNet & every op.\ incl.\ pool & images (class clouds) & MFT manifold dim.\ $D_M$ & -- & \checkmark & -- & non-monotone; pool $\downarrow$ \\
Stephenson et al. \cite{stephenson2021geometry} & VGG-16, ResNet-18; CIFAR-100 & layer out & images & MFT $D_M$ & -- & \checkmark & \checkmark & deep $\downarrow$ late \\
Konz et al. \cite{konz2024preprocessing} & VGG-13/16/19, ResNet-18/34/50; 11 datasets & pool / block / FC & images & TwoNN ID & -- & -- & -- & hunchback (peak 0.57 vs 0.35) \\
Schulte and R\"ugamer \cite{schulte2026rethinking} & AlexNet, VGG, ResNet; ImageNet & layer out & images & TwoNN, MLE (+theory) & -- & -- & -- & cannot rise (theory) \\
\midrule
\multicolumn{9}{@{}l}{\emph{Linear rank / PCA / participation ratio}} \\
Garg et al. \cite{garg2019lowEffort} & VGG/AlexNet-style; CIFAR & conv out, flattened & positions & \#PC at 99.9\% var. & -- & -- & -- & expand to layer 7, then contract \\
Elmoznino and Bonner \cite{elmoznino2024highDim} & 46 DNNs, 568 conv layers & conv out, GAP & images (10k) & PR & -- & \checkmark & -- & rising \\
Chen and Bonner \cite{chen2025universal} & ResNet-18 $\times$20 seeds, ResNet-50 $\times$9 tasks & ReLU out, GMP & images (72k) & rank (matrix\_rank) & -- & \checkmark & -- & not reported \\
Agrawal et al. \cite{agrawal2022alphaReq} & VGG-13/16/19, ResNet-50/101, ViT; ImageNet & pre-pool / block in & images & power-law $\alpha$ & -- & -- & -- & $\alpha<1$ mid, $\to1$ deep \\
Huh et al. \cite{huh2023lowRank} & MLP, CNN, ResNet; CIFAR, ImageNet & penultimate Gram & images & erank & -- & \checkmark & \checkmark & vs.\ net depth $\downarrow$ \\
Smirnov \cite{smirnov2026erank} & ResNet-18 layers 1--4; ImageNet & feature map $HW\times C$ & positions (1 image) & erank & $\min(N,C)$ & -- & -- & per-layer, no profile \\
\midrule
\multicolumn{9}{@{}l}{\emph{Neural collapse / tunnel effect}} \\
Rangamani et al. \cite{rangamani2023intermediate} & MLP, ConvNet, ResNet-18/34/50; MNIST, CIFAR-10, SVHN & centred within-class feat. & images & stable rank & -- & -- & \checkmark & rise then fall \\
Masarczyk et al. \cite{masarczyk2023tunnel} & MLP, VGG-19, ResNet-34 (conv2); CIFAR, CINIC & flattened (8k feats) & images & num.\ rank ($10^{-3}\sigma_1$) & -- & -- & \checkmark & collapse to \#classes \\
Harun et al. \cite{harun2024tunnelVariables} & VGGm, ResNet, ViT; ImageNet-100 & flattened & images & num.\ rank (app.) & -- & -- & -- & collapse at $32^2$, kept at $224^2$ \\
Kubaty et al. \cite{kubaty2025multiExit} & ResNet-20/34/50, ViT; CIFAR-100, ImageNet & activation matrix & images & num.\ rank & -- & -- & regimes & high early, low deep \\
\midrule
\multicolumn{9}{@{}l}{\emph{Other objects (for contrast)}} \\
Feng et al. \cite{feng2022rankDiminishing} & ResNet-18/50, Swin, ViT; ImageNet & \emph{Jacobian} & images & partial num.\ rank & -- & -- & -- & monotone $\downarrow$ \\
Lin et al. \cite{lin2020hrank} & VGG-16, ResNet-50; CIFAR, ImageNet & single $H\times W$ map & per channel & SVD rank & $\min(H,W)$ & -- & -- & per-channel \\
Daneshmand et al. \cite{daneshmand2020bn} & MLP; VGG-19, ResNet-50; CIFAR-10 & last hidden layer & batch & soft rank & width $d$ & \checkmark & \checkmark & vs.\ net depth \\
This work & 19 checkpoints, 11 families; ImageNet, CIFAR-10, COCO & conv out, pre-act., pre-residual (block out and BN out recorded) & positions $\times$ images & $k^*(\tau)$, PR, erank & $r_{\max}$ (and $C$) & \checkmark & \checkmark & Section~\ref{sec:results} \\
\bottomrule
\end{tabular}
\end{table*}

The deep end of any such profile is partly determined analytically: neural collapse \cite{papyan2020neuralCollapse} confines the penultimate-layer features of a converged classifier to a subspace of dimension $C_{\rm class}-1$, nine on CIFAR-10 and $999$ on ImageNet-1k, so a ten-class profile is pushed toward a terminal collapse that a thousand-class profile need not show, and Masarczyk \emph{et al.} \cite{masarczyk2023tunnel} find that on CIFAR-10 the numerical rank of the deeper layers of VGG-19 falls to about the number of classes within the first few hundred steps of training and stays there (the ``tunnel''). Profiles from the two regimes cannot be compared without saying so. Two further results bear on the manifold estimators specifically: Cohen \emph{et al.} \cite{cohen2020manifolds} show with mean-field manifold theory that pooling operations reduce the dimension of class manifolds in AlexNet and VGG-16, and Schulte and R\"ugamer \cite{schulte2026rethinking} show that a Lipschitz layer cannot raise intrinsic dimension, so that a rising TwoNN profile reflects the estimator; neither constraint applies to the linear rank of a covariance, which a linear layer can raise up to $r_{\max}$ and which we measure here.

One nearby result should not be confused with these. Feng \emph{et al.}
\cite{feng2022rankDiminishing} report a rank that decreases monotonically with
depth, but the object measured there is the numerical rank of the network's
Jacobian---a property of the map, not of the activation covariance. Similarly,
the rank used by HRank \cite{lin2020hrank} is the singular-value rank of an
individual $H\times W$ feature map for one channel and one image, which is a
spatial quantity and not the channel covariance spectrum.

The bound we use in Section~\ref{sec:stats} to normalise by attainable rather
than nominal width is not new. That a layer's output rank cannot exceed its
input dimension was made explicit for the softmax layer by Yang \emph{et al.}
\cite{yang2018softmax}, for attention heads whose size is below the sequence
length by Bhojanapalli \emph{et al.} \cite{bhojanapalli2020lowRank}, and for
convolutional expansion layers by Han \emph{et al.} \cite{han2021rexnet},
who plot the rank of $1\!\times\!1$, $3\!\times\!3$ and inverted-bottleneck
outputs against $C_{\rm in}/C_{\rm out}$ in random-weight networks and
turn the observation into a design rule (an expansion ratio of at most six).
Kim \emph{et al.} \cite{kim2018rank1} write the standard-convolution form
$\min(C_{\rm in} k_h k_w,\, C_{\rm out})$ in an im2col derivation.
What none of these do, and what the measurement literature above has not done
either, is carry the bound into the denominator of a depth profile measured on
trained networks. Section~\ref{sec:rmax} shows that doing so removes an apparent two-population structure in every bottleneck network measured, and that trained expansion layers are nearly full-rank in their input space.

\subsection{Where the measurement is taken, and what is learned}
\label{sec:where}

The studies above do not hook the same tensor. Garg \emph{et al.}
\cite{garg2019lowEffort} and NORTH\textsuperscript{*} \cite{maile2022north}
read the output of a convolution; Ansuini \emph{et al.}
\cite{ansuini2019intrinsic} and Elmoznino and Bonner
\cite{elmoznino2024highDim} read the output of a residual block, after the
shortcut has been added and the nonlinearity applied. In a bottleneck block
the last convolution is a $1\!\times\!1$ expansion whose output rank is
bounded by its input width; the shortcut is not, so the block output can, and
in Section~\ref{sec:blockout} does, exceed that bound. Han \emph{et al.}
\cite{han2021rexnet} make the complementary point for random networks: the
nonlinearity after an expansion layer raises the rank above the bound again.
Neither choice of tensor is wrong, but a depth profile is a different object
depending on which was taken, and the published profiles do not say which
comparisons across them are meaningful.

A second question the profile literature leaves open is how much of a
reported profile is a property of the trained network rather than of the
architecture and the input statistics. Elmoznino and Bonner
\cite{elmoznino2024highDim} note that ``architecture-specific factors can
affect [effective dimensionality] in ways that are independent of learning''
and do not pursue it; Han \emph{et al.} \cite{han2021rexnet} measure rank in
random-weight networks only. Saxe \emph{et al.} \cite{saxe2014exact} show,
for deep linear networks, that the singular structure of the representation
is set by the input--output covariance and the initialisation together, which
makes the comparison of a trained profile with its own initialisation the
natural control. Ansuini \emph{et al.} \cite{ansuini2019intrinsic} report one such comparison: the PC-ID profile of an untrained VGG-16 is ``qualitatively the same'' as the trained one (their Fig.~5C), while the TwoNN intrinsic dimension changes shape. Section~\ref{sec:learned} makes the comparison for eleven architecture families, with several trained checkpoints and several random initialisations each, and finds that their observation holds for plain, basic-residual and dense families and fails for bottleneck ones.

A third literature bears on the same quantity from the weight side.
Orthogonality regularisers penalise the distance of a layer's kernel matrix $W \in \mathbb{R}^{C_{\rm out} \times d}$, $d = C_{\rm in} k_h k_w$ \cite{bansal2018orthogonality,huang2018own}, or of its convolution operator \cite{wang2020ocnn}, from an isometry; when $C_{\rm out} > d$ the $C_{\rm out}$ filters cannot be mutually orthogonal, since $WW^{\top}$ has rank at most $d$, and Massart \cite{massart2022orthogonal} characterises what each penalty minimises instead (the spectral restricted
isometry penalty, for instance, is minimised by any $W$ whose singular values
lie in $[0, \sqrt{2}]$). Huang \emph{et al.} \cite{huang2018own} prove that an orthogonal $W$ decorrelates its outputs if its input is white (their Theorem 1), and Huang \emph{et al.} \cite{huang2020oni} list that property only for $C_{\rm out} \le d$ (their Theorem 1), so an over-complete layer is not decorrelated even by a white input. At the tensor we measure, before the
nonlinearity, the output covariance is exactly $W \Sigma_{\rm patch} W^{\top}$,
with $\Sigma_{\rm patch}$ the $d \times d$ covariance of the receptive-field
patch; the effective width of a layer is therefore a joint property of the
kernel spectrum that this literature regularises and of the patch covariance
that it assumes white. Section~\ref{sec:decompose} measures the two factors separately.

\subsection{Spectral decay and the choice of statistic}
\label{sec:mp}

The eigenspectra of convolutional representations are approximately
power-law: Ghosh \emph{et al.} \cite{ghosh2022powerLaws} fit
$\lambda_i \propto i^{-\alpha}$ to intermediate-layer feature covariances,
and Kong \emph{et al.} \cite{kong2022v1Eigenspectrum} find $\alpha$ well
below unity for conventionally trained networks. A spectrum that slow has no
bulk edge separating signal from noise, so a Marchenko--Pastur threshold has
nothing to cut at, and we use variance-fraction and entropy-based statistics
with an explicit threshold sensitivity instead. The rank estimators in current use---the participation ratio and its finite-sample correction \cite{chun2026estimating}, effective rank \cite{roy2007effectiveRank}, RankMe \cite{garrido2023rankme} and the exponent $\alpha$ itself \cite{agrawal2022alphaReq}---weight the head and the tail of such a spectrum differently, and Section~\ref{sec:metrics} compares them on identical layers. (Heavy-tailed \emph{weight}
spectra \cite{martin2021implicitSelfReg} are a different object and do not
transfer.) Pospisil and Pillow \cite{pospisil2025revisiting} identify, for
the comparable spectrum in visual cortex, two estimator biases that apply
here---sampling noise in the covariance, and the fact that $n$ observations
yield at most $n$ nonzero eigenvalues---and Section~\ref{sec:samplesize}
addresses both; we use no cross-validated estimator, so their third does
not arise.

\subsection{Minimum width, lazy training, and training-free proxies}
\label{sec:widththeory}

Three lines of theory concern width without concerning the quantity
measured here. Approximation theory asks how narrow a network can be and
remain a universal approximator: width $n + 4$ suffices and width $n$ does
not for ReLU networks on $\mathbb{R}^n$ \cite{lu2017expressive}, the uniform
threshold is $d_{\rm in} + 1$ \cite{hanin2019universal}, general activations
need $n + m + 2$ \cite{kidger2020universal}, and the exact $L^p$ threshold is
$\max\{d_x + 1, d_y\}$ \cite{park2021minimum}, with $\max\{d_x, d_y\}$ as a
universal lower bound \cite{cai2023achieve}. These are worst-case statements
over a function class in the input and output dimensions, two orders of
magnitude below the channel counts of practical networks, and no
convolutional analogue in terms of channels and kernel support is known to
us. Infinite-width theory \cite{jacot2018ntk,lee2020wide} describes the lazy
regime in which features, and so the rank of every layer's output, stay at
their initial values; feature learning is what moves them
\cite{yang2021feature}, and Vyas \emph{et al.} \cite{vyas2023feature} find
that once it does, representations are consistent across widths, so that a
narrower trained network predicts a wider one. Training-free architecture
scores use quantities available at initialisation, the overlap of activation
codes \cite{mellor2021naswot}, pruning saliencies \cite{tanaka2020synflow,abdelfattah2021zero},
or the kernel's condition number and the number of linear regions
\cite{chen2021tenas}; each gives one score per network for ranking
candidates, and none estimates the width a given layer will use after
training. Rank-collapse theory gives the behaviour at initialisation:
Daneshmand \emph{et al.} \cite{daneshmand2020bn} prove that batch
normalisation keeps the rank of a deep linear network at least of order the
square root of its width, and report that on CIFAR-10 stochastic gradient
descent ``preserves the initial magnitude of the rank to a large extent''
in the last hidden layer. The closest a-priori per-layer quantity is the
rank ratio of randomly initialised expansion layers that Han \emph{et al.}
\cite{han2021rexnet} use as a design rule. Section~\ref{sec:learned}
measures what these leave open: across the convolutional layers of ImageNet
networks, how much of the trained width was already set at initialisation.

\subsection{Redundancy, pruning, and the selection of width}

That trained convolutional networks carry substantial redundancy is well
established. Suau \emph{et al.} \cite{suau2020filterDistillation} compress
layers by analysing the correlation structure of filter responses; Waleffe and
Rekatsinas \cite{waleffe2023pcn} exploit low-rank activation structure that is
present early in training; Nanda \emph{et al.}
\cite{nanda2023diffusedRedundancy} show the redundancy is diffuse, in that a
random subset of neurons in a pretrained layer suffices for downstream tasks.
None of these reports a normalised depth profile.

One network-growing method uses a statistic of the same family as ours. NORTH\textsuperscript{*} \cite{maile2022north} triggers growth on the $\epsilon$-numerical rank of a layer's post-activation
matrix, normalised by that layer's width, and applies this to convolutional
filters in VGG-11 and WideResNet-28 on CIFAR. The statistic is therefore not novel to this work. What that work does not do, and what this paper contributes, is report the quantity as
a profile across depth, compare it against the alternative summary statistics
and estimators in use, measure it at ImageNet scale on pretrained checkpoints,
or contrast it against an untrained control. We also note their own conclusion
that a maximum layer size remains necessary in their setting to prevent
unbounded growth without extensive hyperparameter tuning.

% =========================================================================
\section{Method}
\label{sec:method}

Table~\ref{tab:notation} collects the notation used from here on; each symbol is defined where it first appears.

\begin{table}[t]
  \caption{Notation.}
  \label{tab:notation}
  \centering
  \scriptsize
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{0.34\columnwidth}>{\raggedright\arraybackslash}p{0.62\columnwidth}@{}}
    \toprule
    Symbol & Meaning \\
    \midrule
    $N$, $C$, $H \times W$ & images in the batch; channels and spatial size of a layer's output \\
    $p$; $n$ & positions sampled per image; observations, $n = Np$ (pooled: $n = N$) \\
    $\hat{\Sigma}^{\rm pos}$, $\hat{\Sigma}^{\rm pool}$ & position-sampled and globally pooled channel covariance, Eqs.~\eqref{eq:covpos}--\eqref{eq:covpool} \\
    $\lambda_1 \ge \cdots \ge \lambda_C$ & eigenvalues of the estimated covariance \\
    $k^*(\tau)$ & principal components reaching a fraction $\tau$ of the variance, Eq.~\eqref{eq:kstar}; $\tau = 0.95$ primary, $0.999$ functional \\
    PR, erank & participation ratio and effective rank, Eqs.~\eqref{eq:pr}--\eqref{eq:erank} \\
    $C_{\rm in}$, $C_{\rm out}$, $k_h \times k_w$, $g$ & input and output channels, kernel size and groups of a convolution \\
    $d = C_{\rm in} k_h k_w$ & dimension of the receptive-field patch as a vector (dense convolution) \\
    $r_{\max}$ & attainable rank of the convolution output, Eq.~\eqref{eq:rmax}; the denominator of the ruler \\
    $n/C \ge 50$ & the sample-size gate (Section~\ref{sec:samplesize}); rows below it are not reported \\
    $W \in \mathbb{R}^{C_{\rm out} \times d}$ & the kernel as a matrix; $W = P W_{\circ}$ its polar decomposition, $W_{\circ}$ the nearest isometry \\
    $\Sigma_{\rm patch}$; $\Sigma_{\rm out}$ & patch covariance; convolution-output covariance, $\Sigma_{\rm out} = W \Sigma_{\rm patch} W^{\top}$ (Section~\ref{sec:decompose}) \\
    $\kappa = \sigma_{\max} / \sigma_{\min}$ & condition number of $W$ over its numerical rank \\
    $\Sigma_{\rm act}^{(\ell)}$ & post-activation channel covariance feeding layer $\ell + 1$ (Proposition~\ref{prop:depth}) \\
    $\rho$; T--T, T--R, R--R & Spearman correlation between two layer profiles; trained--trained, trained--random, random--random \\
    $\rho_{\rm depth}$; subscript stage & correlation with depth index; correlation over ranks taken within each stage \\
    step (jitter) & median absolute difference between adjacent layers of one profile \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{The measured object}
\label{sec:object}

Let a convolutional layer produce an output tensor
$Y \in \mathbb{R}^{N \times C \times H \times W}$ over a batch of $N$ images.
Writing $y_{n,:,h,w} \in \mathbb{R}^{C}$ for the vector of channel responses at
one spatial position of one image, the object of interest is the $C \times C$
covariance of that vector.

Two estimators of it appear in the literature, and they are not the same
object. The \emph{position-sampled} estimator draws $p$ spatial positions
uniformly at random from each image and treats each as one observation, giving
$n = Np$:
\begin{equation}
  \hat{\Sigma}^{\text{pos}}
  = \frac{1}{n-1}\sum_{n,(h,w) \in \mathcal{P}_n}
    \bigl(y_{n,:,h,w}-\bar{y}\bigr)\bigl(y_{n,:,h,w}-\bar{y}\bigr)^{\!\top}.
  \label{eq:covpos}
\end{equation}
The \emph{globally-pooled} estimator first averages each feature map over its
spatial extent and treats each image as one observation, giving $n = N$:
\begin{equation}
  \hat{\Sigma}^{\text{pool}}
  = \frac{1}{N-1}\sum_{n}
    \bigl(\tilde{y}_n-\bar{\tilde{y}}\bigr)
    \bigl(\tilde{y}_n-\bar{\tilde{y}}\bigr)^{\!\top},
  \quad
  \tilde{y}_n = \frac{1}{HW}\sum_{h,w} y_{n,:,h,w}.
  \label{eq:covpool}
\end{equation}
Pooling removes the within-image spatial variance rather than summarising it,
so the two estimators can order layers differently. Garg \emph{et al.}
\cite{garg2019lowEffort} used a spatial estimator and Elmoznino and Bonner
\cite{elmoznino2024highDim} a pooled one. The latter report, in their
supplementary S6 Text, that their conclusions about encoding performance
survive when dimensionality is instead computed on the entire flattened
feature map; but that object is $CHW$-dimensional with images as
observations---neither Equation~\eqref{eq:covpos} nor
Equation~\eqref{eq:covpool}---and the dimensionality values under the two
treatments are not reported. To our knowledge the pooled and
position-sampled channel covariances have not been compared on the same
layers. We compute both.

Equation~\eqref{eq:covpool} also has a consequence that matters for
Section~\ref{sec:samplesize}: under pooling, $n$ is the number of
\emph{images}, so a layer with $C=2048$ observed over $10^4$ images is
estimated at $n/C \approx 5$.

\subsection{Summary statistics}
\label{sec:stats}

Let $\lambda_1 \ge \cdots \ge \lambda_C \ge 0$ be the eigenvalues of the
estimated covariance. We report:
\begin{align}
  k^*(\tau) &= \min\Bigl\{k : \textstyle\sum_{i\le k}\lambda_i
               \big/ \sum_i \lambda_i \ \ge\ \tau \Bigr\},
               \label{eq:kstar}\\
  \mathrm{PR} &= \Bigl(\textstyle\sum_i \lambda_i\Bigr)^{\!2}
                 \Big/ \textstyle\sum_i \lambda_i^2 ,
               \label{eq:pr}\\
  \mathrm{erank} &= \exp\Bigl(-\textstyle\sum_i p_i \log p_i\Bigr),
                    \quad p_i = \lambda_i \big/ \textstyle\sum_j \lambda_j ,
               \label{eq:erank}
\end{align}
(The pipeline also records the stable rank and a fitted power-law exponent; neither is used in this paper.) Equation~\eqref{eq:kstar} is the statistic of
\cite{garg2019lowEffort} and Equation~\eqref{eq:pr} that of
\cite{elmoznino2024highDim}; Equation~\eqref{eq:erank} is the effective rank of
Roy and Vetterli \cite{roy2007effectiveRank}, which like PR requires no
threshold but, unlike PR, responds to the tail of the spectrum. Each is divided by a width, since the question is what fraction of the available width is used; which width is the subject of the next paragraph.

A layer's output at one spatial position is a linear map of its
receptive-field patch, so the rank of $\hat{\Sigma}$ is bounded above by the
rank of that map and not by $C$ alone
\cite{kim2018rank1,han2021rexnet}. The bound holds at the point we record
the convolution output---before any nonlinearity and before a residual
addition; a subsequent ReLU or skip connection can raise the rank above it
\cite{han2021rexnet}. For a convolution with $C_{\rm in}$
input channels, kernel $k_h \times k_w$ and $g$ groups, the weight matrix is
block-diagonal with blocks of disjoint support, so the block ranks add and
\begin{equation}
  r_{\max} = g \cdot \min\!\Bigl(
     \tfrac{C_{\rm in}}{g} k_h k_w,\ \tfrac{C_{\rm out}}{g}
  \Bigr).
  \label{eq:rmax}
\end{equation}
Where $r_{\max} < C$, the ratio $k^*/C$ is bounded above by $r_{\max}/C$
whatever the network has learned, and normalising by $C$ therefore charges the
layer for width it never had. The case is not hypothetical: a bottleneck's
$1\!\times\!1$ expansion has $C_{\rm in} = C_{\rm out}/4$, so $k^*/C$
there cannot exceed $0.25$ by construction. We report $k^*/C$, which is the
pre-registered statistic and the one comparable with prior work, and
$k^*/r_{\max}$ alongside it. Section~\ref{sec:rmax} shows what the choice of
denominator does.

Following the pre-specified analysis plan, $k^*(0.95)/C$ is the primary
outcome and all other statistics are secondary. We note that
\cite{garg2019lowEffort} used $\tau = 0.999$, a looser criterion that is more
exposed to the tail of the spectrum and hence to sampling bias; we use that
value only when comparing directly against their published table.

\subsection{Estimation}

Covariances are accumulated in a single pass using the pairwise update of Chan
\emph{et al.} \cite{chan1983variance}, with the running mean and scatter held
in double precision. This is not a formality. Accumulating uncentred outer
products in single precision loses several significant digits, and the first
casualties are the small eigenvalues---which are exactly what $k^*(\tau)$
depends on for $\tau$ near one. The same update lets partial results computed
on separate machines be combined exactly.

The choice of $p$ in Equation~\eqref{eq:covpos} requires comment. Setting
$p = HW$, that is, using every spatial position, maximises the nominal sample
count but not the information: activations at adjacent positions are strongly
correlated, so $NHW$ overstates the number of independent observations. Since
the sample-size analysis of Section~\ref{sec:samplesize} turns on that count,
using all positions would flatter it. We therefore subsample positions and
report $n = Np$ as the sample size, which is conservative.

\subsection{Models and controls}
\label{sec:models}

\begin{table*}[t]
  \centering
  \caption{The first measurement: 22 public ImageNet-1k checkpoints on the
  full validation split: 19 trained networks, the two random initialisations that appear as the \emph{Untrained} column, and one ResNet-50 checkpoint also loaded through a second library as a backend check, counted once.
  \emph{Conv} and \emph{DW} count dense and depthwise convolutional layers
  passing the sample-size gate; $r_{\max}\!<\!C$ counts layers whose
  attainable rank (Equation~\eqref{eq:rmax}) is below their channel count;
  $n/C$ is the range of samples per channel under the position-sampled
  estimator, against a reporting threshold of $50$. Generated from
  \texttt{results/} by \texttt{checkpoint\_analysis.py}.}
  \label{tab:models}
  \footnotesize
  \input{table_models}
\end{table*}

Two measurements were made. The first covers 19 publicly released ImageNet-1k checkpoints of twelve architectures from \texttt{torchvision} and \texttt{timm}, every convolutional layer of each, on the full ImageNet validation split of $50{,}000$ images \cite{deng2009imagenet} (Table~\ref{tab:models}); with two untrained controls, and one ResNet-50 checkpoint loaded through both libraries as a check on the backend (it agrees layer for layer and is counted once), that is 22 measurement runs. They were
chosen for three purposes. Six are ResNet-50 \cite{he2016resnet} trained
under different recipes (the two \texttt{torchvision} recipes, Wightman's A1
and A3, GluonCV, and semi-supervised pre-training on YFCC100M followed by
fine-tuning), so that the same architecture is observed under six independent
optimisations and a between-checkpoint spread can be put beside every
within-checkpoint difference. Five more exercise the rank bound at expansion ratios other than four: Wide-ResNet-50-2 and ResNeXt-50 (ratio two), and MobileNetV2, MobileNetV3-Large and EfficientNet-B0 (ratio six); ResNet-101 adds 33 further bottleneck blocks at ratio four.
The remainder are VGG-16 with and without batch normalisation, the
architecture of \cite{garg2019lowEffort}; ResNet-18 and ResNet-34, whose
basic blocks contain almost no bounded layer; DenseNet-121
\cite{huang2017densenet}; and ConvNeXt-T \cite{liu2022convnext} under two
pre-trainings. ResNet-50 and ConvNeXt-T were also measured at
initialisation, without training.

The second measurement asks what training changes, and needs several
trained checkpoints and several random initialisations of each architecture
on the \emph{same} images. It was run on a fixed subset of $6{,}400$ validation images, which Section~\ref{sec:samplesize} shows is sufficient for the position-sampled statistic, on eleven families: ResNet-50 (the six recipes above, five random seeds), ResNet-18 (six recipes), ResNet-34 (five), ResNet-101 (five), Wide-ResNet-50-2 (three), ResNeXt-50 (five), VGG-16 (with and without BN), DenseNet-121 (two), MobileNetV2 (one), MobileNetV3-Large (four) and EfficientNet-B0 (five), each with three random seeds unless stated. The inverted-residual counts include the TensorFlow-ported \texttt{timm} checkpoints trained on ImageNet-1k alone, so that those families have same-dataset recipes to compare. Random initialisation uses each library's default initialiser with
a fixed seed. The families span five block types: plain (VGG), basic residual (ResNet-18/34), bottleneck residual at two depths, two widths and two cardinalities (ResNet-50/101, Wide-ResNet-50-2, ResNeXt-50), densely connected (DenseNet) and inverted residual (MobileNetV2/V3, EfficientNet). ConvNeXt-T is excluded from this comparison because it has
only four dense convolutions.

Convolutional and post-activation outputs are recorded separately, since a rectified representation is non-negative and has a different covariance structure; the post-activation spectra are released but not analysed here. For residual networks the block output, after the shortcut addition and the following nonlinearity, is recorded as a third tensor, and batch-normalisation outputs can be recorded on request (Section~\ref{sec:robust}). Depthwise
convolutions are flagged and never placed on the same depth axis as dense
ones: a depthwise layer does not mix channels, so its channel covariance is
a different object. This matters for ConvNeXt-T, which is predominantly
depthwise, and for the inverted-residual families.

The untrained control separates structure the network acquired from
structure implied by the architecture together with the second-order
statistics of natural images \cite{field1987relations}. We note in passing
that any argument about the first convolutional layer is bounded by the
dimension of the input patch itself, and is further complicated by the
observation of Bell and Sejnowski \cite{bell1997ica} that the filters such
a layer learns resemble independent components rather than principal ones.

% =========================================================================
\section{Sample-Size Control}
\label{sec:samplesize}

Every number in Section~\ref{sec:results} is a function of an estimated
covariance, so the first question is not what the profile looks like but at
what sample size it stops moving. We answer that before showing any profile.

Two facts bound the exercise. A sample covariance formed from $n < C$
observations is rank-deficient by construction, so its $k^*$ is a statement
about $n$ and not about the network. Well above that point the small eigenvalues remain downward-biased, and $k^*(\tau)$ for $\tau$ near one depends on exactly those; the participation ratio is biased in the same regime, and Chun \emph{et al.} \cite{chun2026estimating} give a corrected estimator for it. The analysis plan therefore fixed, before the data existed, a
reporting rule: no row with $n/C < 50$ is reported, and every reported
statistic must additionally be shown to have flattened in $n/C$.

The number $50$ was a convention when the plan was written; it has a derivation. Lawley's expansion \cite{lawley1956latentRoots} gives the expected sample eigenvalues to first order in $1/n$, and summing it over the top $k$ gives the bias of the variance fraction that $k^*$ reads.

\begin{proposition}[finite-sample bias of the captured fraction]
\label{prop:gate}
Let $l_1 \ge \cdots \ge l_C$ be the eigenvalues of the sample covariance of $n$ observations with population eigenvalues $\lambda_1 \ge \cdots \ge \lambda_C$, and $\hat F(k) = \sum_{i \le k} l_i / \sum_i l_i$. Then, to first order in $1/n$,
\[
  \mathbb{E}\,\hat F(k) - F(k) \;=\; \frac{1}{n\,\operatorname{tr}\Sigma} \sum_{i \le k} \sum_{j > k} \frac{\lambda_i \lambda_j}{\lambda_i - \lambda_j} \;\ge\; 0,
\]
and if $\lambda_{k+1} \le \lambda_k / 2$ then $\mathbb{E}\,\hat F(k) - F(k) \le (2k/n)\,(1 - F(k)) \le (2C/n)\,(1 - F(k))$.
\end{proposition}

The captured fraction is biased upward, so $k^*(\tau)$ is biased downward, by an amount that at $n/C = 50$ is at most $4\%$ of the residual $1 - \tau$ under a factor-two spectral gap at $k$. The gap is an assumption; the double sum is not, and it can be evaluated on the measured spectra. On the 53 convolutions of ResNet-50 the predicted bias at $n = 50\,C$ and $\tau = 0.999$ is $1.8\%$ of the residual in the median and $3.3\%$ at most, and in the convergence data $k^*(0.999)$ at the $n/C = 50$ checkpoint equals its final value on 40 of the 53 layers and differs by one component on the rest. The gate is therefore the sample size at which the first-order bias of the statistic falls below a few percent of what the statistic measures; it is derived in Appendix~\ref{app:proofs} and checked by \texttt{checkpoint\_analysis.py} (its Section~19).

Under the position-sampled estimator of Equation~\eqref{eq:covpos} with
$p = 16$ positions per image over $50{,}000$ images, each convolutional layer
receives $n = 8 \times 10^5$ observations. This gives $n/C$ between $391$ ($2048$-channel layers) and $50{,}000$ ($16$-channel layers), so every convolutional row in the study clears the gate by a factor of at least seven. Fig.~\ref{fig:conv} shows the convergence itself: metrics are
recomputed at $n/C \in \{1,2,5,10,25,50,100,250\}$ within the same single pass,
so the control costs no extra compute. The flattening criterion, applied literally as a change of more than $0.02$ in $k^*(0.95)/C$ between $n/C = 100$ and $250$, flags 14 of the 703 dense layers in the trained checkpoints. Thirteen of them are single-channel moves at $C \le 32$, where one channel is $0.03$ of the range and the criterion cannot separate drift from resolution; the one genuine case is the last convolution of VGG-16 with BN, which moves by 18 channels ($0.035$). All 14 are kept in the counts of Section~\ref{sec:results} and identified in the released output; removing them changes the principal count of Section~\ref{sec:estimator} from $687/703$ to $679/689$, and no layer-level claim rests on any of them.

The pooled estimator of Equation~\eqref{eq:covpool} is in a different position,
and this is not a detail. Under pooling $n$ is the number of \emph{images}, so
$n = 5 \times 10^4$ and $n/C$ runs from $781$ down to $24.4$. For VGG-16,
ResNet-18 and ConvNeXt-T every pooled row clears the gate; for ResNet-50,
eleven of fifty-three do not, and they are precisely the $1024$- and
$2048$-channel layers. Any comparison of the two estimators on those layers is
therefore confounded with sample size, and we do not make one. This limit
applies to the published pooled measurements as well as to ours: a
$2048$-channel layer cannot be estimated at $n/C \ge 50$ from any dataset with
fewer than $10^5$ images.

The convergence curves say when the statistic stops moving in $n/C$; the
second measurement of Section~\ref{sec:models} says it in images. Six
ResNet-50 checkpoints were measured on both the $6{,}400$-image subset
($n/C = 50$ at $C = 2048$) and the full $50{,}000$ images. Over all 53
layers of all six, the two profiles agree with Spearman $\rho \ge 0.999$ and
a maximum absolute difference in $k^*(0.95)/r_{\max}$ of $0.016$. For the
position-sampled statistic the reporting gate is therefore not merely
necessary but close to sufficient, and the several-seed, several-recipe
comparisons of Section~\ref{sec:learned} could be run on the subset without
loss. The pooled statistic does not share this property and was not taken
from the subset.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig3_convergence_resnet50.pdf}
  \caption{Sample-size control, ResNet-50: $k^*(0.95)/C$ against samples per
  channel, one line per convolutional layer, shaded light to dark with depth.
  The dashed rule is the pre-registered reporting threshold $n/C = 50$. Curves
  are flat well before it; the measurement is not sample-limited. Under the
  pooled estimator the same layers would reach only $n/C = 24$ at the right-hand
  edge of this axis.}
  \label{fig:conv}
\end{figure}

% =========================================================================
\section{Results}
\label{sec:results}

The analysis plan fixed, before any ImageNet data were collected, the
primary statistic ($k^*(0.95)/C$), the sample-size gate, and four hypotheses
about the \emph{shape} of the depth profile: that the shape would depend on
the summary statistic, on the estimator, on the class count, and on the
threshold $\tau$. Each shape hypothesis was to be decided by a rank
correlation over the last third of the layers. That rule turned out to have
almost no power (for VGG-16 it is a correlation over four points, with an
exact one-sided $p$ of $0.167$ for the strongest ``collapse'' in the data)
and we do not use it. Every claim below is instead a paired comparison over
\emph{all} gated layers of a network, and every effect size is given beside
the adjacent-layer step of the curve it is measured on. Each subsection is labelled \emph{pre-registered} or \emph{exploratory}, and names which of the two measurements of Section~\ref{sec:models} it draws on. Every number is produced by a script from the released spectra,
not transcribed.

\subsection{Validation against a published profile}
\label{sec:repro-garg}

Before any new profile is read, the protocol was checked against the one
published table it can be compared with directly. Garg \emph{et al.}
\cite{garg2019lowEffort} report $k^*(0.999)/C$ for the thirteen
convolutional layers of a VGG-16 with batch normalisation trained on
CIFAR-10 \cite{krizhevsky2009cifar}. We trained the same architecture on the
same dataset for 100 epochs (one-cycle schedule; test accuracy $94.1\%$) and
measured it with the protocol of Section~\ref{sec:method} at their
threshold. Over all thirteen layers the two profiles agree with Pearson
$r = 0.981$, Spearman $\rho = 0.988$ and a mean absolute difference of
$0.052$. The two largest disagreements are at \texttt{features.27} ($0.73$
ours against $0.53$ theirs) and \texttt{features.30} ($0.52$ against
$0.31$); their training recipe is not fully specified, so agreement to the
digit was not expected, and the criterion we set for passing, $r > 0.9$ with
a mean absolute difference below $0.10$, is a criterion on shape.

The gate is deliberately same-dataset. Our first attempt compared their
CIFAR-10 table with our ImageNet-trained VGG-16 and obtained $r = 0.125$
over the same thirteen layers: the last three layers of an ImageNet VGG sit above $0.9$ where theirs sit at $0.07$--$0.08$. That number is not a failure of either measurement. It says that a CIFAR-10 profile and an ImageNet profile of the same architecture are different objects, and it is why every cross-architecture comparison in this paper is made within one dataset.

A second, ImageNet-scale check was added after the data were seen. Elmoznino and Bonner \cite{elmoznino2024highDim} release the participation ratio of the globally pooled output of every residual block of the \texttt{torchvision} ResNet-50, computed on $10{,}000$ validation images resized to $224\times224$ without cropping. Our pooled block-output participation ratio for the same weights on $50{,}000$ centre-cropped images matches theirs over all 16 blocks with Spearman $\rho = 0.997$ and a median absolute difference in $\log$ of $0.063$ (maximum $0.112$), ours higher in 15 of 16 by a median factor of $1.065$; for the same architecture at initialisation the agreement is $\rho = 0.985$. The images and the resizing differ, so this is agreement between two implementations of one estimator, not a reproduction, and only the seven blocks with $C \le 512$ pass our pooled sample-size gate on their side (maximum difference $0.098$ there). Together with the CIFAR-10 gate it fixes the pipeline against both published estimators at both tensors.

\subsection{Normalising by attainable rank}
\label{sec:rmax}

\emph{Exploratory; full validation set.} Under the nominal denominator ResNet-50 has the lowest median $k^*(0.95)/C$ of the first-round networks ($0.25$ against $0.42$ for VGG-16 and $0.51$ for ResNet-18) and a profile that alternates between adjacent layers in a sawtooth. Grouping its layers by width makes the alternation legible (median $0.23,
0.34, 0.40, 0.37$ for $C = 64 \ldots 512$, then $0.11$ and $0.13$ for
$C = 1024$ and $2048$) and invites the reading that the widest layers use a tenth of the width they are given. Twenty of its 53 convolutions are the
$1\!\times\!1$ expansions at the end of a bottleneck, with
$C_{\rm in} = C_{\rm out}/4$ and hence $r_{\max}/C = 0.25$ by
Equation~\eqref{eq:rmax}; they cannot exceed $0.25$ however well they are
trained.

That the bound explains the reading, rather than merely capping it, is a
testable statement, and the 22 checkpoints test it three ways.

\emph{The bound is never exceeded.} $k^*(0.999)/r_{\max}$ must not pass
unity if $r_{\max}$ is computed correctly. Its maximum over all layers of
all 22 checkpoints lies between $0.960$ and $1.000$. (Equation~\eqref{eq:rmax}
was got wrong twice during this work, once by omitting the kernel extent
and once in the grouped case; this check caught both.)

\emph{Bounded layers track the bound one-for-one.} Pooling the bounded
layers of all trained checkpoints and grouping them by $r_{\max}/C$ gives Table~\ref{tab:bounds}.
\begin{table}[t]
  \centering
  \caption{Bounded layers of all trained checkpoints, grouped by the bound:
  median $k^*(0.999)$ under each denominator. ``Other'' collects the layers
  of DenseNet-121 and MobileNetV3 at bounds between $1/3$ and $3/4$.}
  \label{tab:bounds}
  \footnotesize
\begin{tabular}{crccl}
\toprule
$r_{\max}/C$ & layers & $k^*/C$ & $k^*/r_{\max}$ & families \\
\midrule
$1/6$ & 38  & $0.165$ & $0.990$ & MobileNet, EffNet \\
$1/4$ & 141 & $0.234$ & $0.938$ & ResNet-50/101 \\
$1/2$ & 70  & $0.410$ & $0.820$ & WRN, ResNeXt, other \\
\bottomrule
\end{tabular}
\end{table}

The nominal ratio follows the bound (Spearman $+0.77$ over the 261 bounded layers, medians shown); the attainable ratio does not, and sits at $82$--$99\%$ of
the ceiling. A tighter ceiling is filled more completely, and every value is within the range of the unbounded layers.

\emph{Within a model, the two populations merge.} The formal test is
within each network: are its bounded layers different from its unbounded
ones? Under $C$ they are, in all 14 models that contain expansion layers
(Mann--Whitney $p < 10^{-2}$ in every case, and below $10^{-8}$ in eleven).
Under $r_{\max}$ the difference is gone in all 14 ($p > 0.01$ in every
case; the smallest is $0.025$, for ResNet-34, whose three bounded layers
are downsampling shortcuts). For ResNet-50 the medians move from $0.236$
against $0.938$ to $0.927$ against $0.938$. Under $r_{\max}$ ResNet-50's median $k^*(0.95)$ of $0.42$ is in line with VGG-16 ($0.42$) and ResNet-18 ($0.53$) on the same
images.

The bound is known (Section~\ref{sec:profiles}), and Han \emph{et al.}
\cite{han2021rexnet} observed in random-weight networks that ``drastic
channel expansion harms the rank''. What is added here is a positive finding: in trained networks the expansion layers sit at $94\%$ of the bound at ratio four and $99\%$ at ratio six, so a trained $1\!\times\!1$ expansion is essentially full-rank in its input space, the effect Han \emph{et al.} describe is for these networks the bound and nothing else, and $k^*/C$ at such a layer measures the architecture rather than the representation. The bound is a
necessary condition on the rank, not a sufficient one, and the stem layers
say so: the first convolution of VGG-16 has $r_{\max} = 27$ from a
$3\!\times\!3$ RGB patch, and the MobileNet stems $27$ of $32$, yet these
layers reach only $0.3$--$0.4$ of $r_{\max}$ at $\tau = 0.999$. Natural-image
patches are themselves low-rank; the stem is data-limited, not
architecture-limited, and is the one place where neither denominator is
informative.

The denominator also decides what a comparison with initialisation measures. Against a random initialisation of ResNet-50 (Section~\ref{sec:learned}),
$k^*(0.95)/r_{\max}$ finds no agreement between the trained and the
untrained ordering ($\rho = -0.07$), while $\mathrm{PR}/C$ and effective
rank over $C$ find moderate agreement ($0.58$ and $0.56$), as does
$k^*(0.95)/C$ ($0.55$). The three that agree are the three normalised by
$C$: what they share with the random network is the expansion sawtooth,
which is the architecture. A statistic normalised by $r_{\max}$ removes it.
The lesson is not that one statistic is right, but that the denominator
decides what a trained-versus-random comparison measures.

\subsection{The profiles, and how much they vary between checkpoints}
\label{sec:profile}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig1_profile.pdf}
  \caption{$k^*(0.95)/r_{\max}$ against layer index for the six ResNet-50
  recipes (thin lines) and their five random initialisations (grey), on the
  same $6{,}400$ images. The dashed rule at $1.0$ marks full use of the
  attainable rank. Generated by \texttt{scripts/paper\_figures.py}.}
  \label{fig:profile}
\end{figure}

\emph{Exploratory; full validation set and the $6{,}400$-image subset.} Fig.~\ref{fig:profile} shows the profile, in units of attainable rank (Section~\ref{sec:rmax}), for the case with an error bar: six
ResNet-50 recipes on the same images. Two numbers describe it. Across the
six recipes the per-layer standard deviation of $k^*(0.95)/r_{\max}$ has
median $0.053$ (maximum $0.123$); the adjacent-layer step within any one
recipe has median $0.087$ (range $0.062$--$0.096$). The spread between
independently trained networks is thus about $0.6$ of the step between
neighbouring layers of one network, which settles what may and may not be
read from a single checkpoint. The fine structure of one profile, the value
at one layer against the next, is not reproducible across recipes and is not
reported anywhere below. The ordering across the whole profile is reproducible: pairwise Spearman $\rho$ between recipes is $0.79$--$0.92$. The overall level also moves with the recipe, from $0.38$
(\texttt{torchvision} V1) to $0.48$ (A1), a shift larger than the per-layer
spread; it is a recipe effect and we do no more than note it.

Under the nominal denominator the same six profiles agree more closely
($\rho$ $0.91$--$0.98$), but part of that agreement is the architecture's rank bound, which every checkpoint shares by construction: Section~\ref{sec:rmax} shows that against a random initialisation the agreement is $0.55$ under $C$ and $-0.07$ under $r_{\max}$. Columns $T$ and $R$ of Table~\ref{tab:families} give the median level of each family (its correlation columns are explained in Section~\ref{sec:learned}); across the eleven families the median $k^*(0.95)/r_{\max}$ of a trained network's dense convolutions lies between $0.33$ (Wide-ResNet-50-2) and $0.72$ (MobileNetV2), and the depthwise convolutions of the inverted-residual families lie below their dense ones, at $0.20$--$0.42$. No trained
network in the study uses more than about two thirds of its attainable
channel rank at the $95\%$ criterion, in the median over layers.

\subsection{Pooled versus position-sampled covariance}
\label{sec:estimator}

\emph{Pre-registered; full validation set.} The comparison computes both estimators of
Section~\ref{sec:object} on the same forward pass, the same layers, with the
same spectral machinery, and asks whether they differ in shape. They differ in level.

Over the 19 trained checkpoints, the pooled estimator gives a strictly
lower $k^*(0.95)/C$ than the position-sampled one on $687$ of $703$ dense
convolutions and $79$ of $80$ depthwise convolutions that pass the pooled
sample-size gate. Of the 16 dense exceptions, 14 are ties: ten in DenseNet-121 at $C = 32$, where one channel is $0.031$ of the range and a tie is a matter of resolution, and four at the first bottleneck of four ResNet-50 recipes ($C = 256$). Two are reversals: one channel in DenseNet-121's last dense block, and 23 channels ($0.045$) at one $512$-channel layer of the \texttt{torchvision}-V2 ResNet-50, the only reversal among that checkpoint's 42 gated layers. The same holds for the two untrained controls.

Table~\ref{tab:pooling} gives the size of the effect beside each curve's
own jitter, under both denominators. Under the nominal denominator the three
inverted-residual networks appear to show almost no effect: a median drop
of $0.06$--$0.09$ against an adjacent-layer step of $0.5$--$0.7$. That step
is the $1/6$ expansion sawtooth of Section~\ref{sec:rmax}, an architectural
alternation that the nominal ratio charges to the profile. Under the
attainable denominator the same three networks show a drop of
$1.25$--$2.1$ times their own step, in line with the other families (median
$2.2$). One trained network stays below one under either denominator:
DenseNet-121, whose dense layers alternate a $1\!\times\!1$ convolution of
$128$ channels with a $3\!\times\!3$ of $32$, so that its adjacent-layer
step is structural rather than noise. We report it as it is.

\begin{table*}[t]
  \centering
  \caption{Pooling lowers the level. Per trained checkpoint: dense
  convolutions passing the pooled gate on which the pooled $k^*(0.95)$ is
  strictly lower; the median drop and the profile's own adjacent-layer step,
  under $C$ and under $r_{\max}$; and their ratio. Generated by
  \texttt{checkpoint\_analysis.py}.}
  \label{tab:pooling}
  \footnotesize
  \input{table_pooling}
\end{table*}

Harun \emph{et al.} \cite{harun2024tunnelVariables} report a related architectural effect from the other direction: removing the max-pooling from the first stages of a ResNet removes the rank collapse of its deeper layers. What pooling in the \emph{estimator} does \emph{not} do is reorder the layers. The pre-registered
shape test, a rank correlation between depth and the pooled-minus-sampled
difference over all gated layers, is not significant for any of the three first-round architectures with enough dense layers to test (VGG-16 $\rho = -0.35$, $p = 0.24$; ResNet-18 $+0.26$, $p = 0.26$; ResNet-50 $+0.02$, $p = 0.87$), and for VGG-16 the sign is opposite to the one a
``pooling removes the terminal collapse'' account would need. The
hypothesis that the estimator explains the difference in \emph{shape}
between the two published profiles is therefore not supported. What is
supported is narrower and, we think, more useful: pooling averages away the
within-image spatial variance, and a
profile computed after pooling reads lower at every layer by an amount
larger than the profile's own step. Since \cite{garg2019lowEffort} used a
spatial estimator and \cite{elmoznino2024highDim} a pooled one, the two
literatures' numbers are not on the same scale, whatever their shapes.

Two limits apply. Under pooling $n$ is the image count, so the
$1024$- and $2048$-channel layers of the bottleneck networks fail the gate
even at $50{,}000$ images (eleven of ResNet-50's 53) and are excluded from
the counts above; they are the layers where the difference would be
largest. And the sign test treats layers as independent when adjacent
layers are not, so the per-checkpoint Wilcoxon $p$-values (at most $1.2 \times 10^{-4}$) are optimistic; the count of exceptions is what carries the
claim.

\subsection{Convolution output versus block output}
\label{sec:blockout}

\emph{Exploratory; full validation set.} The bound of Section~\ref{sec:rmax} binds the output of
the expansion convolution. It does not bind the output of the block, which
adds the shortcut to that convolution's output and rectifies the sum. Since
the profile literature reads sometimes one tensor and sometimes the other
(Section~\ref{sec:where}), the two were recorded for every bottleneck block
of the nine bottleneck networks.

In every one of the $161$ blocks, the block output exceeds the rank bound of
its own last convolution. At $\tau = 0.999$ the expansion output has median
$k^*/C$ of $0.23$ (ceiling $0.25$) in the ResNet-50 and ResNet-101
checkpoints, and the block output $0.96$--$0.99$: a factor of $4.1$, which
is the expansion ratio. In Wide-ResNet-50-2 and ResNeXt-50 (ceiling $0.5$)
the factor is $2.3$--$2.4$. The pre-registered statistic behaves the same
way: $k^*(0.95)/C$ rises from $0.09$--$0.19$ at the expansion output to
$0.58$--$0.72$ at the block output, higher in $161$ of $161$.

A profile hooked at block outputs never encounters the artefact of
Section~\ref{sec:rmax}, which is presumably why the studies that hook there
\cite{ansuini2019intrinsic,elmoznino2024highDim} do not report it; a profile hooked at convolution outputs encounters it at every expansion layer, 20 of ResNet-50's 53. Neither is the wrong tensor. But the two profiles differ by a factor of four at those layers, and a depth profile that does not say which tensor it reads is not comparable with one that reads the other. Fig.~\ref{fig:blockout} shows both along the depth of ResNet-50 for the six recipes. The expansion outputs sit at their ceiling of $0.25$ from the second block on; the block outputs rise from $0.72$--$0.92$ in the first block to $0.99$ by the last, so at this tensor and this threshold the block-level profile of a bottleneck network is a shallow rise toward full rank, which is the shape \cite{elmoznino2024highDim} report for ImageNet networks read at this tensor.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig7_blockout.pdf}
  \caption{$k^*(0.999)/C$ along the 16 bottleneck blocks of ResNet-50, six
  recipes, at the output of the last convolution of each block (bounded by
  $r_{\max}/C = 0.25$) and at the block output after the shortcut and the
  nonlinearity. Generated by \texttt{scripts/paper\_figures.py}.}
  \label{fig:blockout}
\end{figure}

\subsection{Do the summary statistics agree?}
\label{sec:metrics}

\emph{Pre-registered; full validation set.} The plan asked whether $k^*(0.95)$ and the participation
ratio, the statistics of the two published profiles, order the layers of
the same network the same way, and predicted that they need not: PR is dominated by the head of the spectrum and $k^*$ counts to its tail. The pre-registered summary was recipe agreement. Across the six
ResNet-50 recipes, the pairwise agreement between recipes is
$\rho = 0.85$ for $k^*(0.95)/r_{\max}$, $0.86$ for $\mathrm{PR}/C$ and
$0.89$ for effective rank over $C$, so each statistic gives a reproducible profile; Fig.~\ref{fig:metrics} shows the three on identical layers of three networks, each divided by $r_{\max}$, offset in level and moving together at the scale of stages.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig2_metrics.pdf}
  \caption{Three statistics on identical layers, each divided by the attainable rank $r_{\max}$: $k^*(0.95)$, participation ratio and effective rank, for one checkpoint each of ResNet-18, ResNet-50 and VGG-16 (BN). The three are offset, since PR and effective rank weight the head of the spectrum more heavily; at the scale of stages they move together, at the scale of layers they do not (Table~\ref{tab:estimators}).}
  \label{fig:metrics}
\end{figure}

\emph{Exploratory (plan Section~11).} Reproducible is not the same as interchangeable, and the pre-registered summary does not test the layer ordering directly. Table~\ref{tab:estimators} does, on the same layers and images, for the ruler and for the rank estimators the surrounding literatures use: the participation ratio, its finite-sample correction (Chun \emph{et al.} \cite{chun2026estimating}, row-corrected form under Gaussianity), the effective rank of Roy and Vetterli \cite{roy2007effectiveRank}, RankMe (Garrido \emph{et al.} \cite{garrido2023rankme}; the same entropy applied to singular values, $\sqrt{\lambda_i}$, instead of eigenvalues) and the power-law exponent $\alpha$ of $\alpha$-ReQ \cite{agrawal2022alphaReq}, all computed from the saved spectra of the $894$ gate-passing dense convolutions of $21$ checkpoints. Within a network, the participation ratio and the ruler do not order the layers the same way: the per-network Spearman correlation between $\mathrm{PR}/r_{\max}$ and $k^*(0.999)/r_{\max}$ has median $0.41$ and reaches $0.8$ in one network of eighteen, and on the ImageNet VGG-16 it is $-0.52$, PR falling from $0.19$ to $0.02$ of $r_{\max}$ over the thirteen layers while $k^*(0.999)$ rises from $0.44$ to $0.96$. Effective rank sits between ($0.63$). RankMe, which halves the exponent of the head, orders layers as the ruler does ($0.94$ against $k^*(0.999)$, $0.99$ against $k^*(0.95)$, above $0.8$ in seventeen networks of eighteen) at a level between the two thresholds ($0.71$ of $r_{\max}$). The exponent $\alpha$ is the most stable quantity across recipes ($0.97$) and correlates negatively with the ruler ($-0.66$), as a decay rate should, but it is a slope and not a count and cannot be put on the width axis. The finite-sample correction of the participation ratio changes it by at most $0.03\%$ at the sample sizes the gate admits, which is the Lawley bound of Section~\ref{sec:samplesize} again. The pooling count of Section~\ref{sec:estimator} is the same under every statistic ($687/703$ for $k^*(0.95)$, $669/703$ for PR, $688/703$ for effective rank).

The pre-registered hypothesis therefore holds in its mechanism and fails in its predicted direction: head-weighted statistics do order layers differently from tail-counting ones, but where the two published profiles differ in shape the PR profile of a plain network falls with depth rather than rising. What the table settles is narrower and more useful: any conclusion of this paper stated for $k^*(\tau)/r_{\max}$ transfers to RankMe and to the pooling count, and does not transfer to the participation ratio or to $\alpha$ without re-measurement.

\begin{table}[t]
  \caption{Estimator comparison on the same layers and images (plan Section~11; $894$ gate-passing dense convolutions, $19$ trained and $2$ random checkpoints). Level: median over layers of the estimator divided by $r_{\max}$ ($^\dagger$: $\alpha$ is a slope, reported raw). $\rho_{0.999}$, $\rho_{0.95}$: per-network Spearman correlation over layers between the estimator and $k^*(0.999)/r_{\max}$ or $k^*(0.95)/r_{\max}$, median across networks (and range); $\ge 0.8$ counts the networks with $|\rho_{0.999}| \ge 0.8$. Recipes: median pairwise $\rho$ between the six ResNet-50 recipes. PR$_{\rm corr}$ is the row-corrected participation ratio of Chun \emph{et al.} \cite{chun2026estimating} in its Gaussian closed form.}
  \label{tab:estimators}
  \centering
  \scriptsize
  \setlength{\tabcolsep}{3.5pt}
  \input{table_estimators}
\end{table}

\subsection{Threshold sensitivity}
\label{sec:threshold}

\emph{Pre-registered; full validation set.} $k^*(\tau)$ needs a threshold, and \cite{garg2019lowEffort} used
$\tau = 0.999$, four orders of magnitude looser in retained variance than the
$0.95$ used here and correspondingly more exposed to the biased tail of the
spectrum. Fig.~\ref{fig:tau} varies $\tau$ over $\{0.9, 0.95, 0.99, 0.999\}$
on VGG-16. The level moves a great deal (the last layer reads $0.29$, $0.44$,
$0.74$ and $0.95$) and the layers rise, dip and recover at the same places.
Across the six ResNet-50 recipes the between-recipe spread is also
threshold-dependent in the expected direction: at $\tau = 0.999$ the
per-layer SD is $0.019$ against an adjacent-layer step of $0.033$, and the
profile mean is $0.86$--$0.92$ of attainable rank, so nearly every layer
is close to full rank and there is little left to order. The threshold
sets how much of the tail is counted.

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig4_threshold_vgg16_bn.pdf}
  \caption{$\tau$ sensitivity on VGG-16. The level is strongly threshold-
  dependent; the ordering of layers is not.}
  \label{fig:tau}
\end{figure}

\subsection{Depthwise and dense convolutions}
\label{sec:dwsplit}

\emph{Exploratory; full validation set and subset.} Section~\ref{sec:models} records depthwise layers
separately on the ground that a layer which does not mix channels has a
channel covariance of a different kind. Three architectures make the
distinction consequential. Eighteen of ConvNeXt-T's 22 convolutions are
depthwise and four are dense, and the two groups do not overlap: median
$k^*(0.95)/C$ of $0.71$ against $0.31$. In MobileNetV3-Large and
EfficientNet-B0 the sign is reversed: the depthwise layers have median
$k^*(0.95)/r_{\max}$ of $0.19$ and $0.16$ against $0.51$ and $0.56$ for the $1\!\times\!1$ layers (columns $T$ of Table~\ref{tab:families}). Interleaved along a single
depth axis, either pair produces a sawtooth that is not a property of the
representation. A profile of such architectures that does not separate the
two is measuring two things at once, and every depthwise number in this
paper is reported on its own.

Kept apart, the two kinds of layer also give ConvNeXt-T a weaker trained-versus-untrained comparison than a pooled test suggests.
Its four dense convolutions are all higher than at initialisation in both
checkpoints, which with four layers is $p = 0.06$; its 18 depthwise
convolutions are higher in $13$ ($p = 0.013$) and $12$ ($p = 0.21$) of 18. We
report the direction and do not claim more. (A first-round test that
pooled the two kinds gave $p = 6 \times 10^{-4}$; it violated the
separation of Section~\ref{sec:dwsplit} and is withdrawn.)

\subsection{What training changes, compared with initialisation}
\label{sec:learned}

\emph{Exploratory; full validation set for the paired test, the $6{,}400$-image subset with several seeds for everything else.}\label{sec:trained} The profiles are not implied by architecture and input statistics alone, and for ResNet-50 the statement has an error bar. Against
one random initialisation on the full validation set, each of the six
trained recipes is higher on $45$ to $50$ of its 53 dense convolutions
(Wilcoxon $p \le 4 \times 10^{-8}$ in every case). The median difference in
$k^*(0.95)/C$ is $+0.10$ to $+0.20$, which is \emph{smaller} than those
profiles' adjacent-layer step of $0.20$--$0.27$; in units of attainable rank, on the $6{,}400$-image subset with five random seeds (Section~\ref{sec:learned}), the median level is $0.23$ at initialisation and $0.47$ trained, against a step of $0.09$. The paired
test over all layers is what carries the claim; the effect size under $C$
would not carry it alone.


That a trained profile is higher than an untrained one says nothing about whether it has the same \emph{shape}. The second
measurement of Section~\ref{sec:models} asks that: for each family, several
trained checkpoints and several random initialisations were measured on the
same $6{,}400$ images, and three rank correlations between $k^*(0.95)/r_{\max}$
profiles were computed over the family's dense convolutions. Random against
random (R--R) is the ceiling a comparison with a single random draw could
reach; trained against trained (T--T) says whether the recipes share a
profile at all; trained against random (T--R) is the question. A family in
which T--R is close to R--R has a profile whose ordering is set by the
architecture; one in which T--R is far below both has learned its ordering.
Table~\ref{tab:families} gives the medians, with the median level of the
trained and random profiles.

\begin{table*}[t]
  \centering
  \caption{Is the ordering of layers learned? Median Spearman $\rho$ between
  $k^*(0.95)/r_{\max}$ profiles over a family's $L$ dense convolutions, on the
  same $6{,}400$ images: random-vs-random over $n_R$ seeds, trained-vs-trained
  over $n_T$ checkpoints, and trained-vs-random over all pairs. $T$ and $R$
  are the median levels. Depthwise rows are reported separately. VGG-16's
  two checkpoints are with and without BN. Generated by
  \texttt{checkpoint\_analysis.py}.}
  \label{tab:families}
  \footnotesize
  \input{table_families}
\end{table*}

Three behaviours appear in the families measured, and they sort by block type; whether the block is the cause is taken up below, with a controlled experiment that says it is not.

\emph{Plain, basic-residual and densely connected networks learn a level, not an ordering.} This is the case Ansuini \emph{et al.} \cite{ansuini2019intrinsic} saw in VGG-16, where the linear PC-ID profile was qualitatively unchanged by training. For VGG-16, ResNet-18, ResNet-34 and DenseNet-121 the
trained profiles agree with the random ones at $\rho = 0.61$--$0.75$, against
ceilings of $0.79$--$0.98$, and training raises the median level by $0.09$ (VGG) to $0.19$ (ResNet-18). The profiles' own adjacent-layer steps are $0.05$, $0.14$, $0.10$ and $0.30$ (Table~\ref{tab:pooling}), so the rise exceeds the step in all but DenseNet-121, whose step is the structural $1\!\times\!1$/$3\!\times\!3$ alternation noted in Section~\ref{sec:estimator}.

\emph{The ImageNet ResNet-50 checkpoints learn their ordering.} The six recipes agree with
each other ($0.84$) and with initialisation not at all ($-0.08$, range
$-0.31$ to $+0.33$ over 30 pairs), against a random-vs-random ceiling of
$0.94$; the five random draws are highly reproducible, so the disagreement
is not draw noise. This does not contradict the observation of Daneshmand \emph{et al.} \cite{daneshmand2020bn} that gradient descent largely preserves the initial rank of the \emph{last} hidden layer: their object is one layer's rank across networks of different depth, ours is the ordering of layers within one network, and the level of ResNet-50 does rise rather than fall. The trend with depth reverses: every random draw falls with depth (writing $\rho_{\rm depth}$ for the Spearman correlation of a profile with layer index: $-0.53$ to $-0.81$) and every trained
recipe rises (mean $+0.56$). Part of that disagreement is the trend itself: a sign flip in one monotone trend gives $\rho \approx 0$ over 53 layers without any reordering inside a stage. Ranking layers within each stage, where all share $C$ and no width or bound confound is possible, and pooling the ranks (column T--R$_{\rm stage}$ of Table~\ref{tab:families}) removes the trend: ResNet-50's within-stage agreement with initialisation is $0.27$ against a ceiling of $0.75$, ResNet-18's is $0.75$ against $0.92$ and ResNet-34's $0.55$ against $0.87$. The bottleneck network reorders its layers within a stage as well, but less completely than the whole-profile figure suggests.

\emph{Inverted-residual checkpoints agree with one another weakly or not at all.} The four MobileNetV3-Large checkpoints do not share a profile (T--T $-0.18$) although their random draws do ($0.75$); the five EfficientNet-B0 checkpoints, all trained on ImageNet-1k alone, agree only moderately ($0.40$, against a random ceiling of $0.97$); MobileNetV2 has a single public checkpoint. What varies most is the level: the median $k^*(0.95)/r_{\max}$ of the $1\!\times\!1$ layers is $0.18$, $0.45$, $0.59$ and $0.65$ across the MobileNetV3 checkpoints and $0.10$, $0.26$, $0.70$, $0.71$ and $0.73$ across the EfficientNet-B0 ones, a $3.6$-fold and a $7$-fold range under one architecture and one dataset. The depthwise layers move against the rest of the study in two of the three families: in the median over checkpoints training roughly halves their effective width in MobileNetV3 ($0.41 \to 0.20$) and EfficientNet-B0 ($0.35 \to 0.22$), with trained values spread from $0.05$ to $0.34$, while MobileNetV2's single checkpoint leaves them where initialisation put them ($0.39 \to 0.42$).

Two further checks bear on the reading. Under the nominal denominator the same comparison gives T--R of $0.57$ for ResNet-50 against $0.67$ (ResNet-34) and $0.81$ (ResNet-18): the contrast survives but shrinks, because what the trained and random bottleneck profiles share under $C$ is the expansion sawtooth, which is the architecture (Section~\ref{sec:rmax}). And the families with an architectural ordering are also the shallower ones, so depth could stand in for block type. Three further bottleneck families separate the two. ResNet-101, twice as deep as ResNet-50, agrees with its initialisation \emph{more}, not less (T--R $0.20$, within-stage $0.32$); Wide-ResNet-50-2 and ResNeXt-50, the same depth as ResNet-50 with expansion ratio two, agree more still ($0.33$ and $0.46$; within-stage $0.57$ and $0.66$). All four bottleneck families lie below every plain, basic and dense family ($0.61$--$0.75$), and the four order as a gradient rather than a class: the ordering is least architectural in the deepest-and-narrowest bottleneck and more so as the block is widened or given cardinality. Depth alone does not produce the effect, since the deeper network shows less of it.

A natural hypothesis, that training reorders the $1\!\times\!1$
channel-mixing layers and leaves the $3\!\times\!3$ layers where the
architecture put them, does not survive the role split. In ResNet-50 both
kinds are reordered (T--R $+0.09$ for $3\!\times\!3$, $-0.28$ for
$1\!\times\!1$); in DenseNet-121 the $3\!\times\!3$ layers are the ones
that move ($-0.26$) while the $1\!\times\!1$ layers stay ($+0.39$). What
sorts the ImageNet families is the block, not the kernel.

\emph{Is the block the cause? Under one recipe on CIFAR-10 it is not.} The
released plan (its Section~9.1) registered, before any of its data were
seen, the experiment that separates the block from everything that varies
between public checkpoints: with the recipe of
Section~\ref{sec:repro-garg}, a basic-block ResNet $[6,6,6,6]$ and a
bottleneck ResNet-50 $[3,4,6,3]$ with the same stage widths
($64$--$512$ at the block input), the same CIFAR stem and the same number of
convolutions ($52$ and $53$), and a CIFAR variant of MobileNetV2, three
seeds each, measured at initialisation, at 17 points along training and at
the end, on the CIFAR-10 test images. The registered hypothesis H9.1 was
that the bottleneck network's trained-versus-random agreement lies below the
basic network's (every pair, or Mann--Whitney $p < 0.01$); its registered
consequence, if it failed, was that the ImageNet contrast is attributed to
depth or recipe and this section is rewritten accordingly.
Table~\ref{tab:round3} gives the outcome. H9.1 fails: the basic network
agrees with its initialisation at $\rho = 0.55$ ($0.46$--$0.57$ over the
nine trained-random pairs), the bottleneck network at $0.50$
($0.38$--$0.54$), $p = 0.2$, and the MobileNetV2 at $0.44$; under one recipe
at this scale the three block types keep the same share of their initial
ordering. The other two ImageNet signatures of ResNet-50 are absent as well:
the level of the CIFAR bottleneck network \emph{falls} on training ($0.20
\to 0.16$; the basic network rises, $0.21 \to 0.32$, as its ImageNet
relatives do), and its depth trend flattens ($-0.78 \to -0.21$) rather than
reversing. The registered attribution therefore stands: what makes the six
ImageNet ResNet-50 recipes reorder their layers is not the bottleneck block
as such but something they share and this experiment lacks, the dataset or the
ImageNet recipes, which the experiment does not separate (Section~\ref{sec:limits}); the four-family
gradient above already excludes depth alone. Two exploratory readings
survive. Within stages the bottleneck network does reorder more than the
basic one (T--R$_{\rm stage}$ $0.25$ against $0.50$), the same direction as
on ImageNet ($0.27$ against $0.75$), but this comparison was not registered
and rests on three seeds. And the second registered hypothesis, H9.2, holds:
the three MobileNetV2 seeds share their trained profile at $\rho =
0.99$--$1.00$ against a random-versus-random ceiling of $0.68$, so an
inverted-residual network trained under one recipe does have a profile, and
the disagreement among the public MobileNetV3 and EfficientNet checkpoints
is the recipes' doing.

\begin{table*}[t]
  \centering
  \caption{The controlled experiment (pre-registered, plan Section~9.1):
  CIFAR-10, one recipe, three seeds per architecture, $k^*(0.95)/r_{\max}$
  over the dense convolutions on the test images. R--R, T--T and T--R as in
  Table~\ref{tab:families}, T--R with its range over the nine pairs;
  $L$ dense convolutions;
  T--R$_{\rm stage}$ ranks within stages; $R \to T$ is the median level at
  initialisation and at the end; $\rho_{\rm depth}$ likewise; settle is the
  first measured epoch at which $\rho$ against the final profile reaches
  $0.95$ (median over seeds); accuracy is the mean final top-1. Generated by
  \texttt{checkpoint\_analysis.py}.}
  \label{tab:round3}
  \footnotesize
  \input{table_round3}
\end{table*}

Three cautions. The threshold sets how much room this comparison has: at $\tau = 0.999$ the
trained-versus-random agreement for ResNet-50 is $0.25$ rather than
$-0.07$, because the ceiling compresses both. The inverted-residual checkpoints differ in more than the optimiser: one MobileNetV3 was pre-trained on ImageNet-21k and the longest-trained checkpoints ran for $3{,}600$ epochs, and even the five same-dataset EfficientNet-B0 checkpoints span three training frameworks. ``Weak agreement'' is therefore a statement about the checkpoints one can download; the same-recipe, several-seed study registered in the released plan (the CIFAR-10 MobileNetV2 of Table~\ref{tab:round3}) finds the seeds agreeing at $0.99$--$1.00$, so it is not a property of the block. That is also the practical content of the finding: a single MobileNet or EfficientNet checkpoint does not stand for its architecture.
And MobileNetV3's random draws agree only at $0.75$ ($0.37$ for the
depthwise layers), so its random profile is itself unstable, possibly a
consequence of its hard-swish nonlinearity and squeeze-excitation blocks.

\emph{When does the profile become the trained one?} One training run of
VGG-16 with BN on CIFAR-10 was measured at 17 points along its 100 epochs
(one seed; a second run of the recipe of Section~\ref{sec:repro-garg}, reaching $94.0\%$; Fig.~\ref{fig:traj}).
The profile leaves its
initialisation in the first epochs ($\rho$ against the initial profile
falls to $0.72$ by epoch 5 while the level overshoots to $0.47$), returns
towards it during the high-learning-rate phase, and is in place by epoch
40: $\rho$ against the final profile is $0.98$ from epoch 40 onwards, while
test accuracy still rises from $84\%$ to $94\%$. The final profile agrees
with the initial one at $\rho = 0.93$, consistent with the plain-network
row of Table~\ref{tab:families} and stronger. For a plain network, then, the ordering of layers is settled before the fit is, and it is largely the ordering the architecture began with (the controlled runs below extend this to the other two block types); the level moves earlier and non-monotonically, as the effective rank of penultimate features does in Huh \emph{et al.} \cite{huh2023lowRank} and the manifold dimension of deep layers does late in training in Stephenson \emph{et al.} \cite{stephenson2021geometry}, and Masarczyk \emph{et al.} \cite{masarczyk2023tunnel} report the rank collapse of the deeper layers within the first few hundred steps. What settles at epoch 40 in our run is the Spearman ordering under a one-cycle schedule whose peak learning rate falls at epoch 30; a different schedule could settle at a different epoch.

The nine runs of the controlled experiment, under the same schedule, give
the same picture with seeds (Table~\ref{tab:round3}, medians over three
seeds). All three architectures leave their initialisation in the first
epoch, and further than VGG-16 does: after one epoch the profile is
\emph{anti}-correlated with the initial one ($\rho = -0.50$ for both
ResNets), the depth trend has reversed from falling ($-0.90$, $-0.78$) to
rising ($+0.69$, $+0.86$) and the level has doubled or tripled ($0.22 \to
0.63$, $0.20 \to 0.37$). The rising trend of the trained ImageNet ResNet-50
is thus present transiently in every CIFAR-10 network, basic or bottleneck,
and is undone during the high-learning-rate phase: by epoch 15--20 the
trend is falling again and the agreement with initialisation has climbed
back to $0.6$--$0.9$, from where it drifts down to its final $0.50$--$0.54$.
The profile then settles at epoch 60 (basic), 40 (bottleneck) and 40
(MobileNetV2) while accuracy is still $84$--$88\%$ on its way to
$93$--$95\%$. The bottleneck trajectory that the VGG-16 run could not supply
therefore shows no late reordering: on CIFAR-10 the bottleneck network's
ordering is in place as early as the others' and ends as close to
initialisation as theirs.

Along this CIFAR-10 run the profile also falls with depth from epoch 10 onwards ($\rho_{\rm depth}$ $-0.6$ to $-0.9$; only $-0.2$ during the first epochs), and the last three layers end at $k^*(0.95)/C$ of $0.05$--$0.07$ ($0.08$ at $\tau = 0.999$): the terminal collapse of \cite{garg2019lowEffort} is present in our own CIFAR-10 network and absent in our ImageNet VGG-16, where the same layers read $0.44$--$0.53$ ($0.92$--$0.97$ at $\tau = 0.999$). Neural
collapse \cite{papyan2020neuralCollapse} predicts a penultimate rank of
$C_{\rm class} - 1$, nine on CIFAR-10, but the CIFAR head is also
shallower than the ImageNet one, and the planned experiment that separates
class count from head depth was not run (Section~\ref{sec:discussion}).

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{fig5_trajectory_grid.pdf}
  \caption{CIFAR-10 training trajectories: rank agreement of the
  $k^*(0.95)/r_{\max}$ profile with its initial and its final form, the
  median level, and test accuracy, against epoch. Top left: VGG-16 (BN), one
  run. The other three panels are the controlled architectures of
  Table~\ref{tab:round3}, median over three seeds (thin lines: the seeds).
  Every network leaves its initialisation in the first epoch, the three
  block types most sharply (agreement $-0.5$), recovers part of it during
  the high-learning-rate phase, and has its final ordering in place by epoch
  40--60 while accuracy is still rising. Generated by
  \texttt{scripts/paper\_figures.py}.}
  \label{fig:traj}
\end{figure*}

\subsection{A functional calibration: truncating to the $k^*$ subspace}
\label{sec:projection}

\emph{Exploratory; the $6{,}400$-image subset.} The statistic counts
directions of variance. Whether the directions it discards carry function
can be checked at inference by an operation that is itself well known:
replacing each convolution output $y$ by $\bar y + U_k U_k^{\top}(y - \bar y)$,
with $U_k$ the top-$k$ principal channel directions of that output, is the
response-PCA truncation of Zhang \emph{et al.}
\cite{zhang2015efficient,zhang2016accelerating} (their Eq.~(4), the linear
form of their method), which they fold into the kernel as a low-rank
factorisation and apply, with a per-layer rank chosen under a speed budget
and usually followed by fine-tuning, to SPP-net and VGG-16. Energy
thresholds as a rank rule appear there and as baselines in later work
\cite{liebenwein2021alds}, and Garg \emph{et al.} \cite{garg2019lowEffort}
retrain at their $99.9\%$ criterion. The closest precedent for reading a dimension count off accuracy is the ``classification dimension'' of Feng \emph{et al.} \cite{feng2022rankDiminishing}, the number of principal components of a layer that preserves $95\%$ of accuracy, measured one layer at a time. We use the truncation for one purpose only: with $k = k^*(\tau)$ from the measurement, at every dense convolution at once, with no fine-tuning and no rebuilding, does accuracy survive?

What the answer should depend on is fixed to first order by the discarded variance.

\begin{proposition}[first-order effect of truncation]
\label{prop:truncation}
Let $z$ be the output of layer $\ell$ at a position, $\Sigma_\ell = \sum_i \lambda_i u_i u_i^{\top}$ its covariance, $r = (I - U_k U_k^{\top})(z - \bar z)$ the discarded component, and $J_\ell$ the Jacobian of the logits with respect to $z$. Then $\mathbb{E}\|r\|^2 = (1 - \tau_k)\operatorname{tr}\Sigma_\ell$ with $\tau_k$ the captured fraction, and to first order
\[
  \mathbb{E}\|J_\ell r\|^2 = \operatorname{tr}\bigl(J_\ell \Sigma_\ell^{\perp}(k) J_\ell^{\top}\bigr) \le \|J_\ell\|_{\rm op}^2 (1 - \tau_k) \operatorname{tr}\Sigma_\ell,
\]
where $\Sigma_\ell^{\perp}(k) = \sum_{i > k} \lambda_i u_i u_i^{\top}$; truncating $L$ layers at once perturbs the logits by at most $\sum_\ell (\mathbb{E}\|J_\ell r_\ell\|^2)^{1/2}$ in root mean square.
\end{proposition}

The proposition does not predict an accuracy, since that depends on the logit margins and on the nonlinearity beyond first order, but it fixes the scale: between $\tau = 0.999$ and $0.95$ the discarded energy per layer differs by a factor of $50$ and its amplitude by $7$, and the layers add. A criterion that keeps $0.1\%$ of the variance at each of 53 layers and one that keeps $95\%$ are therefore not two settings of one knob but an order of magnitude apart in what they inject, which is what the measurement below finds. The proof is in Appendix~\ref{app:proofs}. The first-order term was evaluated directly on the A1 recipe (64 held-out images, covariances from 256 others): for each of the 53 convolutions, the logit change under truncation of that layer alone was compared with its linearisation along the discarded component. Across layers the two agree in rank order at $\rho = 0.99$--$1.00$ at every $\tau$, and the linear term overestimates the actual change by a factor of $1.2$--$1.3$ in the median, so the first-order picture holds even at $\tau = 0.95$ where the perturbation is large. The layers that dominate are the same three the diagnostic of Section~\ref{sec:diagnostic} finds under-used, the stem, the first expansion of stage~1 and its projection shortcut, and the second of them is the layer the leave-one-layer-out test below identifies. At $\tau = 0.999$ the largest single-layer effect ($98$) is of the order of the median squared logit margin ($55$), which is where an accuracy loss of the size this recipe shows becomes possible; at $0.95$ it exceeds the margin by two orders of magnitude.
On one half of the subset the channel covariance of every convolution output of eight checkpoints (the six ResNet-50 recipes, ResNet-18, VGG-16 with BN) was accumulated exactly as in the measurement; on the other half the network was evaluated with every convolution output truncated. The control truncates each layer to $k^*(\tau)$
random orthonormal directions instead. Table~\ref{tab:projection} gives top-1 accuracy, and since truncated and untruncated networks are scored on the same images, each difference has an exact McNemar test; ``unchanged'' below means that test does not reject at $p = 0.01$.

\begin{table}[t]
  \centering
  \caption{Top-1 on $3{,}200$ held-out images with every convolution output truncated to its top $k^*(\tau)$ principal channel directions, all layers at once, no fine-tuning. Truncating to $k^*(\tau)$ random orthonormal directions instead gives at most $2\%$ in every case. $^*$: McNemar $p < 0.01$ against the untruncated network at $\tau = 0.999$. Generated by \texttt{checkpoint\_analysis.py} from \texttt{projection\_check.py}.}
  \label{tab:projection}
  \scriptsize\setlength{\tabcolsep}{3.5pt}
  \input{table_projection}
\end{table}

At $\tau = 0.999$ the truncation keeps $84$--$93\%$ of each layer's
attainable rank ($63$--$86\%$ of nominal width), and in six of the eight
checkpoints accuracy is unchanged: ResNet-50 under the \texttt{torchvision}
V1 and V2, GluonCV and semi-supervised recipes, ResNet-18 and VGG-16 move
by $-0.3$ to $+0.2$ points (McNemar $p \ge 0.15$). The two exceptions are
the ResNet-50 checkpoints trained with the A1 and A3 recipes of Wightman
\emph{et al.} \cite{wightman2021rsb}, which lose $1.7$ points each
($p < 10^{-4}$). The same architecture, the same data and the same $k^*$
therefore give different answers depending on the recipe: the RSB recipes
(binary cross-entropy, heavy augmentation) place function in channel
directions that hold less than $0.1\%$ of the variance, and the
conventional recipes do not. In every checkpoint the directions beyond
$k^*(0.999)$ carry at most a few points of accuracy, and random directions
of the same dimension give chance throughout, so what is retained is the
principal subspace and not merely the dimension count.

Below $\tau = 0.999$ the cost depends on the recipe far more than on the
architecture. At $0.99$ four checkpoints lose $1.6$--$5.2$ points while A1
and A3 fall to $11\%$ and $34\%$; at $0.95$, the pre-registered threshold,
ResNet-50 V1, ResNet-18 and VGG-16 lose $19$--$21$ points but the other
five ResNet-50 recipes fall to $0.3$--$6.6\%$. Part of that collapse is a
single layer: in the A1 checkpoint \texttt{layer1.0.conv3} holds more than
$99\%$ of its variance in one direction, so it is truncated to one
dimension at $\tau \le 0.99$, and leaving that layer untruncated raises
the A1 network from $11\%$ to $54\%$ at $0.99$; it does not change the
$1.7$-point loss at $0.999$, which is spread over layers. The functional
reading of $k^*$ is therefore threshold-specific and recipe-specific:
$k^*(0.999)$ bounds the width used by conventionally trained networks and
undercounts it by about two points of accuracy for the RSB recipes, and
$k^*(0.95)$, which is what the profiles above are drawn with, orders layers
and tracks training but says nothing about how much width could be
removed.

Two further things this experiment does not show. Read as a compression,
the truncation is the rank-$k$ factorisation $W \mapsto U_k (U_k^{\top} W)$,
which costs $k(d + C_{\rm out})$ multiply-adds per position against
$C_{\rm out} d$; summed over ResNet-50's dense convolutions at
$\tau = 0.999$ it removes only $5\%$ of the convolution FLOPs, because the
$1\!\times\!1$ expansion layers have $d < C_{\rm out}$ and gain nothing from
factorisation, and Zhang \emph{et al.}'s nonlinear and fine-tuned variants
do better at any given speed-up. And it is not a pruning result: a rank
restriction on a trained network is not a narrower network trained from
scratch, and whether the latter would reach the same accuracy is a
different question, which we do not address.

\subsection{Robustness of the protocol}
\label{sec:robust}

\emph{Exploratory; the $6{,}400$-image subset and $3{,}200$ COCO images.}
Each choice the protocol makes was varied on ResNet-50 (V1) and the
$k^*(0.95)/r_{\max}$ profile compared with the reference run by Spearman
$\rho$ across layers and by the per-layer difference
(Table~\ref{tab:ablation}). Four choices do not matter at the resolution
of one channel: sampling 4 or 64 positions per image instead of 16,
resizing the image directly to $224 \times 224$ instead of cropping it, and
reading the batch-normalisation output instead of the convolution output
all give $\rho \ge 0.99$ and a median per-layer difference below $0.01$,
the largest single difference being one channel of a 64-channel layer.
The batch-norm result deserves a word, since a per-channel affine map can
in principle reshape the spectrum: in the trained network it does not,
and in the untrained one the two spectra coincide. The protocol's numbers
are therefore not artefacts of the sampling, the preprocessing or the
exact tensor within a conv--BN pair.

Two variations do matter, and they say what the profile is a property of.
Replacing the images by noise changes the trained profile substantially:
white noise gives $\rho = 0.32$ and a median level of $0.24$ against
$0.42$, and $1/f$ noise, which shares the second-order statistics of
natural images, recovers part of it ($\rho = 0.70$, level $0.29$). The
same substitution moves the untrained network's profile much less
($\rho = 0.78$--$0.88$ against its ImageNet profile). A trained profile is
thus a property of the network on natural images, not of the network
alone, which is the data factor of Section~\ref{sec:decompose} seen from
the other side. Changing the natural images themselves, on the other
hand, changes nothing: the same ImageNet-trained ResNet-50 measured on
$3{,}200$ COCO images gives a profile identical to its ImageNet profile
to within one channel at every layer. Finally, the ResNet-50 bodies of
the COCO-trained Faster R-CNN and Mask R-CNN detectors, measured on the
same COCO images, order their layers like the classification network
($\rho = 0.91$) but at a lower level ($0.29$ and $0.31$ against $0.41$):
the detection task, or its training schedule, uses less of the attainable
width at this criterion. The protocol reads no labels, so it applies to
any convolutional backbone unchanged.

\begin{table*}[t]
  \centering
  \caption{Robustness of the $k^*(0.95)/r_{\max}$ profile of ResNet-50 (V1)
  to the protocol's choices, and its transfer to other images and to
  COCO-trained backbones. $\rho$: Spearman across layers against the
  reference run (6,400 ImageNet images, 16 positions, centre crop,
  convolution output); $|\Delta|$: per-layer absolute difference. The noise
  rows for the random initialisation are compared with that
  initialisation's ImageNet profile. Generated by
  \texttt{checkpoint\_analysis.py}.}
  \label{tab:ablation}
  \footnotesize
  \input{table_ablation}
\end{table*}

\subsection{Where the effective width comes from}
\label{sec:decompose}

\emph{Exploratory; the $6{,}400$-image subset.} At the tensor this paper
reads, the output of a convolution before its nonlinearity, the channel
covariance is an exact function of two things: the kernel, and the
covariance of the receptive-field patch. Writing $W \in
\mathbb{R}^{C_{\rm out} \times d}$ for the kernel as a matrix, $d = C_{\rm in}
k_h k_w$, and $\Sigma_{\rm patch}$ for the $d \times d$ covariance of the
patch vector (the im2col column) at a sampled position,
\begin{equation}
  \Sigma_{\rm out} = W\, \Sigma_{\rm patch}\, W^{\top}.
  \label{eq:decomp}
\end{equation}
The bound of Section~\ref{sec:rmax} is the rank of the right-hand side. Equation~\eqref{eq:decomp} also separates the two factors exactly, and the separation is what the counterfactual below rests on. Write $W = P W_{\circ}$ for the polar decomposition, with $P = (WW^{\top})^{1/2}$ positive semidefinite and $W_{\circ}$ the nearest semi-orthogonal matrix to $W$ (for $W = U S V^{\top}$, $W_{\circ} = U V^{\top}$), and let $r = \operatorname{rank} W$ and $\sigma_{\max} \ge \sigma_{\min} > 0$ be the largest and the smallest nonzero singular value of $W$.

\begin{proposition}[the kernel reweights the counterfactual spectrum]
\label{prop:ostrowski}
Let $\Sigma_{\circ} = W_{\circ} \Sigma_{\rm patch} W_{\circ}^{\top}$. Then
$\Sigma_{\rm out} = P \Sigma_{\circ} P$, and for $i \le r$ there are
$\theta_i \in [\sigma_{\min}^2, \sigma_{\max}^2]$ with
$\lambda_i(\Sigma_{\rm out}) = \theta_i\, \lambda_i(\Sigma_{\circ})$; both
spectra vanish beyond $r$.
\end{proposition}

\begin{corollary}
\label{cor:kstar}
With $\kappa = \sigma_{\max}/\sigma_{\min}$, $\tau' = \kappa^2\tau / (1 - \tau + \kappa^2 \tau)$ and $\tau'' = \tau / (\kappa^2 (1 - \tau) + \tau)$,
$k^*(\tau''; \Sigma_{\circ}) \le k^*(\tau; \Sigma_{\rm out}) \le k^*(\tau'; \Sigma_{\circ})$ for every $\tau \in (0, 1)$.
If the nonzero singular values of $W$ are equal ($\kappa = 1$) then
$k^*(\tau; \Sigma_{\rm out}) = k^*(\tau; \Sigma_{\circ})$, and if moreover
$C_{\rm out} \ge d$ then $k^*(\tau; \Sigma_{\rm out}) = \min(k^*(\tau; \Sigma_{\rm patch}), r_{\max})$.
\end{corollary}

\begin{proposition}[the counterfactual is a compression of the data]
\label{prop:compression}
$\Sigma_{\circ}$ is unitarily similar to the compression of $\Sigma_{\rm patch}$
to the row space of $W$, so $\lambda_{i + d - r}(\Sigma_{\rm patch}) \le
\lambda_i(\Sigma_{\circ}) \le \lambda_i(\Sigma_{\rm patch})$ for $i \le r$, with
equality of the nonzero spectra when $r = d$. In particular $\Sigma_{\circ}$
depends on the kernel only through its row space, not through its singular
values.
\end{proposition}

\begin{proposition}[the data factor along depth]
\label{prop:depth}
Let $\Sigma_{\rm act}^{(\ell)}$ be the channel covariance of the
post-activation output of layer $\ell$ at a position, and suppose the
position-sampled patch of layer $\ell + 1$ stacks $k_h k_w$ such vectors,
each almost surely in the range of $\Sigma_{\rm act}^{(\ell)}$. Then
$\operatorname{rank} \Sigma_{\rm patch}^{(\ell+1)} \le k_h k_w\,
\operatorname{rank} \Sigma_{\rm act}^{(\ell)}$, and hence
$\operatorname{rank} \Sigma_{\rm out}^{(\ell+1)} \le \min\bigl(C_{\ell+1},\,
k_h k_w \operatorname{rank} \Sigma_{\rm act}^{(\ell)}\bigr)$.
\end{proposition}

Proofs are in the appendix; they are linear algebra (the Ostrowski and
Cauchy interlacing theorems). Proposition~\ref{prop:ostrowski} says that the
measured spectrum is the counterfactual spectrum reweighted eigenvalue by
eigenvalue by factors confined to the kernel's singular-value range, so the
kernel acts on the width only through the spread of its singular values,
and Corollary~\ref{cor:kstar} is the exact form of the statement that an
isometric kernel passes the effective width of its input through unchanged
(the orthogonality-regularisation literature's aim, Section~\ref{sec:where},
which reaches the ceiling only if the input is also white).
Proposition~\ref{prop:depth} is the second bound on a layer's width, set by
the data and the preceding layer rather than by the architecture; it is the
bound that binds at initialisation. The propositions give the four quantities below their meaning, and they are checked on the data. Over the 465 dense convolutions of the ten checkpoints in Table~\ref{tab:decompose}, with the numerical rank of $W$ taken at $10^{-6}$ of its largest singular value, the eigenvalue ratios of Proposition~\ref{prop:ostrowski} lie within $[\sigma_{\min}^2, \sigma_{\max}^2]$ on 464 (the exception is a DenseNet-121 layer whose smallest retained singular value sits at the tolerance), and the inequality of Corollary~\ref{cor:kstar} holds on 465 at both $\tau = 0.95$ and $0.999$. The corollary is informative, that is, gives a bound below $r_{\max}$, on 246 of the 465 layers at $\tau = 0.95$; on the rest the condition number $\kappa$ is large enough that $\tau'$ is close to one. The tolerance is not doing the work: repeating the ResNet-50 check at $10^{-4}$ and $10^{-8}$ gives 53 and 51 of 53 layers within the Ostrowski bounds (the two exceptions at $10^{-8}$ are the stem and the first convolution of stage 1, two of the six layers whose numerically null directions that tolerance retains), the corollary holds on every layer at every $\tau$ at all three settings, and the median $\kappa$ is $10.6$ throughout; only its maximum moves, from $166$ to $10^{8}$. Twenty-four layers of the trained checkpoints have numerically null kernel directions (in ResNet-50 all six are in the stem and the first stage), and none of the random initialisation does: training empties some kernel directions outright, which the polar factor $W_{\circ}$ simply omits.

We accumulated $\Sigma_{\rm patch}$ for every dense convolution of ten
checkpoints on the subset ($n/d \ge 67$ throughout) and compared four
quantities per layer: the measured $k^*/r_{\max}$, recomputed from
Equation~\eqref{eq:decomp}; the same statistic for $W W^{\top}$, which is
what the layer would show if its patches were white; the statistic for
$\Sigma_{\rm patch}$ itself, capped at $r_{\max}$; and the statistic for
$W_{\circ}\Sigma_{\rm patch}W_{\circ}^{\top}$, where $W_{\circ}$ is the polar
factor of $W$, the nearest (semi-)isometry to the trained kernel. The last
is an analytic counterfactual: the effective width this layer would have if
its kernel were orthogonalised and its input left as it is.
Table~\ref{tab:decompose} gives the medians and the rank correlations across
layers; Fig.~\ref{fig:decompose} shows ResNet-50 layer by layer.

\begin{table*}[t]
  \centering
  \caption{Decomposition of $k^*/r_{\max}$ by Equation~\eqref{eq:decomp}, medians
  over a checkpoint's $L$ dense convolutions on the $6{,}400$-image subset.
  \emph{out}: measured; \emph{kernel}: $WW^{\top}$ (white patches);
  \emph{data}: $\Sigma_{\rm patch}$ capped at $r_{\max}$; \emph{ortho}:
  orthogonalised kernel with the measured patches. $\rho$ columns are Spearman
  correlations across layers with \emph{out}. The random initialisation is one
  seed. Generated by \texttt{checkpoint\_analysis.py}.}
  \label{tab:decompose}
  \footnotesize
  \input{table_decompose}
\end{table*}

\begin{figure}[t]
  \centering
  \includegraphics[width=\columnwidth]{fig6_decompose_trained.pdf}
  \includegraphics[width=\columnwidth]{fig6_decompose_random.pdf}
  \caption{The four quantities of Table~\ref{tab:decompose} for every dense
  convolution of ResNet-50, trained (top) and at initialisation (bottom). At
  initialisation the measured width follows the patch covariance and the
  kernel is nearly isotropic; after training the patch covariance is full-rank at this criterion (the cap at $r_{\max}$ is trivial for $3\!\times\!3$ layers, where $d > r_{\max}$, and binding for $1\!\times\!1$ layers, the dips) and the measured width follows the kernel.
  Generated by \texttt{scripts/paper\_figures.py}.}
  \label{fig:decompose}
\end{figure}

Three things follow. First, the identity is a check on the pipeline:
recomputing $k^*(0.95)/r_{\max}$ from $W$ and an independently accumulated
$\Sigma_{\rm patch}$ reproduces the directly measured value to within
$0.008$ on every layer of the five checkpoints where both exist.

Second, training moves the bottleneck from the data to the kernel. At
initialisation the patch covariance is far from full-rank (median
$0.28$ of $r_{\max}$ for ResNet-50), the random kernel is nearly isotropic
($0.86$), and the measured width follows the data ($\rho = 0.94$) and not
the kernel ($0.44$); orthogonalising the random kernel changes nothing ($0.22$ against $0.20$). The patch covariance of the untrained network also loses rank with depth (Fig.~\ref{fig:decompose}, bottom), which is why every random profile in Section~\ref{sec:learned} falls with depth: at initialisation the profile is a property of how the input statistics degrade through random layers, not of the kernels. In every trained checkpoint the patch covariance
has become full-rank at the $95\%$ criterion (median $1.00$; $0.88$--$0.89$
for the two inverted-residual networks), and the measured width follows the kernel spectrum ($\rho = 0.81$--$0.97$ for ResNet-18, the three ResNet-50 recipes, VGG-16 and DenseNet-121; $0.57$ for ResNet-34; $0.46$ and $0.70$ for EfficientNet-B0 and MobileNetV3) and more weakly the data ($0.24$--$0.63$ for the first six; $0.67$ and $0.78$ for the two inverted-residual networks, whose patch covariance is the one that is not saturated). The ordering of layers in a trained
profile is, to a first approximation, the ordering of the kernels' Gram
spectra.

Third, the level is set jointly. A trained kernel alone, under white
patches, would give $0.69$--$0.86$; the measured value is $0.42$--$0.70$;
orthogonalising the kernel with the measured patches gives
$0.75$--$0.87$, higher than the measured value in $401$ of the $412$ dense convolutions of the nine trained checkpoints at $\tau = 0.95$. The
trained kernel is therefore not merely anisotropic; its high-gain
directions are aligned with the high-variance directions of its input, so
that the two factors compound and the output spectrum is steeper than
either would make it alone. The alignment can be measured directly: with
$W = \sum_i s_i u_i v_i^{\top}$, let $\rho_{\rm align}$ be the Spearman
correlation, over the retained right singular directions, between the
kernel's gain $s_i^2$ and the input variance the direction carries,
$v_i^{\top} \Sigma_{\rm patch} v_i$; it is zero for an isotropic kernel or
white input, positive when the kernel amplifies the input's principal
directions, negative when it suppresses them.

\emph{The alignment, measured (plan Section~9.6; the registered sign was wrong).} The plan registered, before the data, that trained kernels would show $\rho_{\rm align} < 0$, reasoning from the counterfactual above; the reasoning had the sign backwards, since it is amplification of the principal directions, not suppression, that steepens the output spectrum below the isometric counterfactual. As registered the prediction fails in every checkpoint ($0$ of $48$). What the data show, with the same test in the other direction, is reported here as a post-hoc finding. Over the $19$ distinct ImageNet checkpoints of Table~\ref{tab:models}, on the $6{,}400$-image subset at $48$ positions, the median $\rho_{\rm align}$ over a network's dense convolutions is $+0.91$ in the median and above $+0.8$ in $16$ of $19$; the six ResNet-50 recipes give $+0.86$ to $+0.95$, and the sign test is significant at $p < 0.01$ in $43$ of the $48$ trained networks measured (the $19$ plus $29$ CIFAR-10 networks: the $24$ of Section~\ref{sec:learned}, the final networks of Section~\ref{sec:a18}'s arms and the trajectories). At initialisation it is zero: every one of the $20$ random draws has $|\rho_{\rm align}| < 0.1$ in the median. The exceptions are the inverted-residual families, MobileNetV2 $+0.11$ (not significant), MobileNetV3-Large $+0.28$ and EfficientNet-B0 $+0.61$, which are also the families of Section~\ref{sec:learned} whose trained checkpoints share no profile. Along training the alignment rises rather than falls: on the nine CIFAR-10 runs of Section~\ref{sec:a18} measured at seventeen epochs, the layer-median $\rho_{\rm align}$ correlates with epoch at $+0.65$ to $+0.91$, reaches $90\%$ of its final value by epoch $7$--$15$ in every run, before the profile itself settles (epochs $15$--$100$), and is within $10\%$ of its final value at the settle epoch in seven of nine. The orthogonality penalties of Section~\ref{sec:learned} do not undo it: paired over the $195$ layers of the SO and untreated arms, $|\rho_{\rm align}|$ differs by $+0.002$ in the median ($p = 0.5$, P9.12 not supported), which is consistent with those penalties leaving the kernels far from isometric. Across families the trained value is not a constant ($+0.11$ to $+0.98$; P9.13), so it is a property that training produces to different degrees, not a fixed point. Fig.~\ref{fig:decompose} and Table~\ref{tab:decompose} are unchanged by this; what changes is the reading: the trained kernel is a matched filter for its input's principal directions, and the measured width sits below the isometric counterfactual because the kernel sharpens the input spectrum rather than flattening it.

This is the quantity the orthogonality-regularisation literature acts on
from the weight side (Section~\ref{sec:where}): Huang \emph{et al.} \cite{huang2018own} show that an orthogonal kernel decorrelates its output when the input is white, and Table~\ref{tab:decompose} shows
that in trained networks the input is not white but is full-rank, so
orthogonalisation would raise the effective width by about $0.3$ of
$r_{\max}$ rather than to the ceiling. That number is a prediction, made
here with the kernels held at their trained values and the patches held
fixed; a network trained with an orthogonality penalty changes both.

\emph{The prediction, tested.} The released plan (its Section~9.2)
registered the test before its data were seen. VGG-16 (BN) and the
basic-block ResNet of Table~\ref{tab:round3} were trained on CIFAR-10 under
the recipe of Section~\ref{sec:repro-garg} with no penalty, with the soft
orthogonality penalty SO ($\|WW^{\top} - I\|_F^2$, $\lambda = 10^{-4}$) and
with SRIP ($\lambda = 10^{-2}$), the strengths of Bansal \emph{et al.}
\cite{bansal2018orthogonality}, three seeds each; for every dense
convolution the measured $k^*(0.95)/r_{\max}$ of the penalised network is
compared with the \emph{ortho} counterfactual of the unpenalised network of
the same seed. Three outcomes were registered: P9.3, that the SO arm lands
within $0.1$ of the counterfactual in the median over layers; P9.4, that
SRIP does not (its minimisers need not be isometries
\cite{massart2022orthogonal}); and P9.5, that the width gained bears no monotone
relation to accuracy. Table~\ref{tab:ortho} gives the result.

\begin{table*}[t]
  \centering
  \caption{Orthogonality penalties against the counterfactual
  (pre-registered, plan Section~9.2): CIFAR-10, three seeds, medians over
  seeds of per-network medians over the $L$ dense convolutions passing
  $n/d \ge 50$. \emph{none}, \emph{penalised}: measured $k^*(0.95)/r_{\max}$;
  \emph{predicted}: the \emph{ortho} counterfactual of the unpenalised
  network; \emph{realised}: the fraction of the predicted gain obtained;
  $\rho$(gain, pred.): Spearman across layers between the gain obtained and
  the gain predicted; kernel erank: effective rank of $W$ over $r_{\max}$,
  unpenalised $\to$ penalised. Generated by
  \texttt{checkpoint\_analysis.py}.}
  \label{tab:ortho}
  \footnotesize
  \input{table_ortho}
\end{table*}

P9.3 fails. Both penalties raise the level, from $0.35$--$0.36$ to
$0.53$--$0.56$ under SO and $0.42$--$0.54$ under SRIP, which is half of the
predicted gain (median fraction realised $0.50$ under SO, $0.24$ and $0.43$
under SRIP), leaving the SO arm $0.15$--$0.17$ below the counterfactual of
$0.71$--$0.73$. The shortfall has a visible cause. At these strengths the
penalties do not make the kernels isometric: their effective rank rises
from $0.53$--$0.56$ of $r_{\max}$ to $0.80$--$0.85$, and the counterfactual
recomputed from the penalised kernels themselves is still $0.72$--$0.76$
against their measured $0.42$--$0.56$, so the anisotropy that remains is
aligned against the input as it was before. The alternative the plan named,
that the penalty moves $\Sigma_{\rm patch}$, has no room at this threshold:
the patch covariance is full-rank at the $95\%$ criterion in every arm.
What the counterfactual does predict is the \emph{ordering} of the gains.
Across the layers of the SO networks the gain obtained correlates with the
gain predicted at $\rho = 0.72$ (ResNet, 41 layers) and $0.91$ (VGG-16, 8
layers); across the SRIP networks it does not ($-0.14$ and $-0.19$), which is
the form in which P9.4 holds. P9.5 holds as registered: over the twelve
penalised runs accuracy moves by $-0.1$ to $+0.3$ points, uncorrelated with
the width gained ($\rho = 0.15$). The counterfactual is therefore an upper
bound that a soft penalty approaches by about half, in the right layers; the
direct test, an exact isometry constraint such as ONI \cite{huang2020oni},
was not run.

\subsection{Reading the profile as a diagnostic}
\label{sec:diagnostic}

\emph{Exploratory; not pre-registered.} What is a profile for, once it is
calibrated? Two uses are within reach of the data already in hand, and
both are stated as readings, not results.

\emph{Where a trained ResNet-50 leaves width unused.} Table~\ref{tab:diagnostic} pools the six recipes: for every convolution it takes the median of $k^*(\tau)/r_{\max}$ over the recipes, then the median over the layers of one role in one stage. At $\tau = 0.999$, the criterion the truncation of Section~\ref{sec:projection} showed to be the width the classifier uses, 29 of the 53 layers are above $0.9$ in every recipe, and every $3\!\times\!3$ convolution of every stage is at $0.95$--$0.97$. The width that is not used sits at the entry of the network and in its projection shortcuts: the stem uses half of its 64-dimensional attainable rank ($0.49$; its input is a $7\!\times\!7$ RGB patch, Section~\ref{sec:rmax}), the first block of stage 1 uses $0.40$--$0.61$, and the four $1\!\times\!1$ stride-2 projection shortcuts use $0.61$, $0.72$, $0.85$ and $0.94$ from stage 1 to stage 4. These four layers and the stem are the only ones below $0.7$ in all six recipes. At $\tau = 0.95$ every role reads about one half; that number is the same in every stage and, by Section~\ref{sec:projection}, is not a width one can remove. The reading a designer takes from the table is therefore not ``ResNet-50 is half idle'' but ``its entry and its shortcuts are wider than what passes through them, by a factor of about two, in every recipe''.

\begin{table}[t]
  \centering
  \caption{Six-recipe consensus for ResNet-50, $k^*(0.999)/r_{\max}$ / $k^*(0.95)/r_{\max}$: the median over recipes of each layer, then the median over the layers of a role within a stage; $6{,}400$ images. Generated by \texttt{checkpoint\_analysis.py}.}
  \label{tab:diagnostic}
  \footnotesize\setlength{\tabcolsep}{3.5pt}
  \input{table_diagnostic}
\end{table}

\emph{The ruler's widths beside a pruning method's.} Reading $k^*(0.999)$ off the trained CIFAR-10 VGG-16 of Section~\ref{sec:learned} on $6{,}400$ training images gives, layer by layer, the widths $14, 47 \mid 107, 121 \mid 238, 247, 249 \mid 456, 376, 261 \mid 40, 40, 40$ (bars separate the five resolutions), $5.1$M parameters against $14.7$M. Network slimming \cite{liu2017slimming}, which learns per-layer widths by an $\ell_1$ penalty on the batch-norm scales and a global threshold, reports for its 16-convolution VGGNet on the same dataset (their supplementary Table~5) $22, 62 \mid 83, 119 \mid 193, 168, 85, 40 \mid 32, 32, 32, 32 \mid 32, 32, 32, 38$, at a test error below the unpruned network's. The two agree where the profile is decided: both cut the first convolution to a fifth or a third of 64, both keep the second and third resolutions nearly whole, and both collapse the last resolution to $32$--$40$ of $512$, the terminal collapse of Section~\ref{sec:learned} read by a different instrument. They disagree at the fourth resolution, where slimming removes $94\%$ and the ruler keeps $50$--$90\%$. The disagreement is informative about what each measures: the ruler reports the rank the trained network's activations actually span, slimming reports what can be removed when the network is retrained around the removal. Whether the ruler's widths retrain to the full network's accuracy is the pre-registered experiment of Section~\ref{sec:a18}: read at the convolution output they do on CIFAR-10 and not on CIFAR-100; read after the ReLU, the tensor the next layer consumes, they do at two thirds of the parameters.

\subsection{Retraining at the widths the ruler names}
\label{sec:a18}

\emph{Pre-registered (plan Section~9.5); CIFAR-10, three seeds per arm.} The widths of Section~\ref{sec:diagnostic} were read off one trained network, not chosen; the plan asked whether a network built at those widths and trained from scratch keeps the accuracy of the full one, and whether the per-layer allocation matters. Four arms of VGG-16 (BN) under the recipe of Section~\ref{sec:learned}: full width; the ruler's widths $\lceil k^*(0.999)_\ell \rceil$ measured on the seed-0 full network over $6{,}400$ \emph{training} images at 16 positions (floor 8), $5.09$M parameters or $34.5\%$ of the full; a uniform cut of every layer to the same parameter count (factor $0.582$); and the ruler at $\tau = 0.95$, $0.63$M parameters. Table~\ref{tab:a18} gives the accuracies and the final level of each arm.

\begin{table}[t]
  \caption{Widths from the ruler, retrained from scratch (pre-registered, plan Sections~9.5, 9.7 and 9.8): VGG-16 (BN), three seeds per arm, test accuracy at epoch 100 as mean $\pm$ SD over seeds, and the median over layers of the retrained network's own $k^*(0.999)/r_{\max}$. MACs are multiply-adds per $32 \times 32$ image; img/s is inference throughput measured on one RTX 4090 at batch 256. The CIFAR-10 full-width arm is the untreated arm of Table~\ref{tab:ortho}.}
  \label{tab:a18}
  \centering
  \scriptsize
  \setlength{\tabcolsep}{2.5pt}
  \input{table_a18}
\end{table}

Three predictions were registered. P9.6, that the ruler's widths lose less than $0.3$ points on average and no seed falls more than $0.5$ below the full-width mean, holds: $93.55$ against $93.77$, a difference of $0.22$, with the lowest seed $0.40$ below. P9.8, that the $\tau = 0.95$ widths lose at least one point, holds with $2.15$ points: the training-side counterpart of Section~\ref{sec:projection}, where truncating a trained network to $k^*(0.95)$ costs it $19$--$21$ points, and the same statement about which threshold names the width in use. P9.7, that every uniform-cut seed falls below every ruler seed, was registered as a strict three-against-three ordering and does not hold as registered: eight of the nine pairs favour the ruler and the ninth is an exact tie, $9{,}337$ correct test images in both networks, so the pre-registered rule is failed by a single image. By the rule's own consequence we do not claim that the per-layer allocation is needed at this budget; the exploratory reading is a mean difference of $0.35$ points in the ruler's favour (uniform $93.20$), with the uniform arm's worst seed $0.46$ below the ruler's. The exploratory P9.9 also fails: the network retrained at the ruler's widths does not fill them ($0.85$ of $r_{\max}$ in the median, against $0.74$ for the full network, $0.92$ for the uniform cut and $0.98$ at the $\tau = 0.95$ widths), so a narrower network uses a larger fraction of its width without using all of it, and the count is re-made by training rather than inherited from the widths.

\emph{A second dataset (plan Section~9.7, registered after the CIFAR-10 result and before any CIFAR-100 data).} The same four arms on CIFAR-100, with the widths re-read from a CIFAR-100 full-width network (Table~\ref{tab:a18}, lower block), give a different answer. The ruler's widths are nearly the full ones in the last five layers ($466$--$490$ of $512$, against $40$ on CIFAR-10: the terminal collapse of Section~\ref{sec:learned} is the ten-class task's, and with a hundred classes the deep layers stay full), so the ruler removes only $13\%$ of the parameters, almost all of it from the first seven layers, whose widths it sets as on CIFAR-10. P9.14, the analogue of P9.6, fails: the ruler arm loses $0.59$ points ($72.64$ against $73.23$) and its lowest seed is $0.82$ below the full mean. P9.15 fails in the direction opposite to P9.7's tie: the uniform cut to the same parameter count is above the ruler arm in nine pairs of nine ($73.24$, indistinguishable from full width). P9.16 holds with $5.8$ points. The network retrained at the ruler's widths does fill them ($0.93$ of $r_{\max}$).

\emph{The tensor the next layer consumes (plan Section~9.8, registered after the CIFAR-100 result and before its data).} The failure has a candidate cause that the measurement itself exposes. The ruler reads the convolution output, before the nonlinearity, because that is where the bound of Section~\ref{sec:rmax} holds; but the layer after it consumes the post-activation tensor, and the ReLU raises the rank (Section~\ref{sec:blockout}). On both full-width networks the first convolution's output spans $14$--$15$ directions at $\tau = 0.999$ and its ReLU output $47$--$48$; the second, $47$--$48$ against $64$; from the third layer on the two agree within $5$--$25\%$ (CIFAR-10's last three layers excepted: $40$ against $168$--$347$). A layer narrowed to the rank of its own output therefore caps at that rank what the next layer receives, and the cap binds in the first two layers, whose input is a low-rank RGB patch. The plan registered a fifth arm, widths read after the ReLU ($47, 64 \mid 127, 128 \mid 255, 255, 254 \mid 504, 486, 469 \mid 347, 285, 168$ on CIFAR-10, $67\%$ of the parameters), with the prediction P9.17 that it retrains within $0.3$ of full width, and P9.18 that it beats the convolution-output widths seed for seed. Both hold: $93.84$ against $93.77$, every seed above every seed of the convolution-output arm (Table~\ref{tab:a18}). On CIFAR-100 the post-activation widths are $99\%$ of the full ones, so by the rule registered in advance that arm is not evidence for P9.17; it retrains to $73.49$.

What the three arms settle together is a statement about where the ruler is read. A width read from a trained network at $\tau = 0.999$ is the rank its activations span; read at the convolution output it is the width the layer \emph{produces}, and a network built at those widths loses accuracy where the nonlinearity had raised the rank before the next layer (CIFAR-100, $-0.59$); read after the nonlinearity it is the width the next layer \emph{consumes}, and a network built at those widths matches full width on CIFAR-10 at two thirds of the parameters. The measurement of this paper is taken at the convolution output because that is where $r_{\max}$ bounds it and where the kernel and the data separate (Section~\ref{sec:decompose}); a design decision must read the same profile one tensor later. The pre-registered claim that the convolution-output allocation beats a uniform cut is not supported on either dataset (a one-image tie, then a reversal); the post-activation allocation beats it on CIFAR-10 in nine pairs of nine, the only dataset on which it is a cut at all. A width read at $\tau = 0.95$ cannot be trained to on either dataset. The cost columns of Table~\ref{tab:a18} say what each cut buys: the post-activation widths remove a third of the parameters but, because the removed width sits in the low-resolution layers, only $12\%$ of the multiply-adds and $6\%$ of the inference time, whereas the uniform cut to the convolution-output arm's parameter count removes two thirds of the multiply-adds; the $\tau = 0.95$ widths run four to eight times faster at a cost of two and six points. None of this makes the ruler a pruning method: nothing is fine-tuned, the comparison is retraining from scratch, and one architecture and two datasets are one case.

\subsection{The same ruler on a transformer}
\label{sec:vit}

\emph{Exploratory; one checkpoint, no initialisation control.} Nothing in the protocol is specific to convolutions: a linear layer is a $1\!\times\!1$ convolution over its input vector, its attainable rank is $\min(d_{\rm in}, d_{\rm out})$, and over a token sequence the tokens play the role of positions. Table~\ref{tab:vit} applies the measurement to a ViT-B/16 \cite{dosovitskiy2021vit} checkpoint (ImageNet-21k pre-training, ImageNet-1k fine-tuning) on the $6{,}400$-image subset with 32 tokens per image, so that the widest layer ($3{,}072$) clears the gate at $n/C = 67$. Two readings. The patch embedding, a $16\!\times\!16$ RGB patch mapped to $768$ dimensions, uses $0.07$ of its attainable rank at $\tau = 0.95$ and $0.22$ at $0.999$: like the ResNet stem, its input is low-rank and the bound is not what binds. And every one of the four linear roles rises with depth, from about a tenth of the attainable rank in the first blocks to one half or more in the last ($\rho$ against block index $0.76$--$0.99$), which is the monotone rise the pooled ImageNet profiles of Section~\ref{sec:related} report, here read at the layer, position-sampled, and against the attainable rank. Whether the rise is architectural or learned is the comparison with initialisation that this paper makes for convolutional families and does not make here; the residual stream and the attention map are not hooked. The extension is in the released package.

\begin{table}[t]
  \centering
  \caption{ViT-B/16 (in21k$\to$in1k), $k^*(\tau)/r_{\max}$ over the twelve blocks by role, median and range at $0.95$, median at $0.999$, and the Spearman correlation with block index; $6{,}400$ images, 32 tokens each. Generated by \texttt{checkpoint\_analysis.py}.}
  \label{tab:vit}
  \scriptsize\setlength{\tabcolsep}{3pt}
  \input{table_vit}
\end{table}

% =========================================================================
\section{Discussion}
\label{sec:discussion}

\subsection{Why the published profiles disagree}
\label{sec:why}

Section~\ref{sec:intro} set out to reconcile a profile that saturates and
collapses \cite{garg2019lowEffort} with one that rises
\cite{elmoznino2024highDim}. The reconciliation is not the one we expected.
The estimator (Section~\ref{sec:estimator}) does not change the ordering of layers, only
the level, by more than the profile's own step, so the two literatures' numbers are not on one scale. The summary
statistic does change the ordering (Section~\ref{sec:metrics}): the participation ratio and the ruler
disagree layer by layer, and on a plain network they run in opposite directions; but the direction
is the reverse of the one that would reconcile the two published shapes, so the statistic explains part of the disagreement and not its sign. The tensor is: a profile read at block
outputs is higher by the expansion ratio at every bottleneck expansion
(Section~\ref{sec:blockout}), and the denominator adds a sawtooth to any
profile read at convolution outputs (Section~\ref{sec:rmax}). And the
terminal collapse is a property of the task: measured with one protocol, our
own CIFAR-10 VGG-16 collapses in its last three layers and our ImageNet
VGG-16 does not (Section~\ref{sec:learned}). The two published profiles
differ, then, in what tensor they read and what task they were trained on,
and would differ in level even if they agreed on both.

\subsection{What a width measurement has to state}
\label{sec:protocol}

The three pitfalls of Sections~\ref{sec:rmax}, \ref{sec:estimator} and \ref{sec:blockout}, and the two further requirements below, are not errors in any published work. Each is a choice
that a measurement must make, and each was made differently by different
studies without the consequence being known. Stated as a protocol, a
reported effective-width profile should say: which tensor is read
(convolution output, post-activation, or block output), because the rank
bound binds the first and not the third and the two differ by the expansion
ratio; whether spatial positions or images are the observations, because
the pooled value is lower at essentially every layer by more than the
profile's own step; what the denominator is, because the nominal width is
unattainable at every expansion layer and a profile divided by it
alternates with the architecture rather than with the representation;
that depthwise and dense layers are on separate axes; and the
sample-to-channel ratio at the widest layer, because under pooling it is
the image count and fails at $2048$ channels for any dataset under
$10^5$ images. None of this is expensive. The whole first measurement took about five hours on one consumer GPU, and the second ran on a laptop.

\subsection{Implications for choosing width}

The descriptive question was how much of its width a trained layer uses. Once the denominator is right the answer has two parts. At the $95\%$ criterion a trained dense convolution uses between $0.33$ and $0.72$ of its attainable rank in the median over layers, across eleven families; that number orders layers and tracks training but is not a count of removable width, since truncating to it costs ResNet-50 $21$ points (Section~\ref{sec:projection}). At the $99.9\%$ criterion it uses $0.84$--$0.93$ of its attainable rank, and in six of eight checkpoints truncating to that costs nothing a paired test detects. For those networks the second number is the one a width-selection method has to explain: a seventh of the attainable rank, and a third of the nominal width, of a conventionally trained ResNet-50 carries no variance its classifier uses. The two RSB-trained checkpoints show that this margin is a property of the recipe: the same architecture trained with binary cross-entropy and heavy augmentation uses about two points' worth of the variance beyond $k^*(0.999)$. It is still not a pruning ratio (Section~\ref{sec:limits}), because a rank restriction on a trained network is not a narrower network trained from scratch; and Section~\ref{sec:a18} shows what the count is good for and where it must be read: a network built at the convolution-output $k^*(0.999)$ widths and trained from scratch matches full width on CIFAR-10 at a third of the parameters but loses $0.6$ points on CIFAR-100, and one built at the post-activation $k^*(0.999)$ widths, the tensor the next layer consumes, matches full width at two thirds of the parameters with every seed above the convolution-output arm; the $k^*(0.95)$ widths lose two and six points.

What differs across architectures is how much of the profile is learned.
For plain and basic-residual networks the ordering of layers is the
architecture's and training adds a level; a method that sets widths from
the architecture alone, or from the network at initialisation, would get
the ordering right for those families. For the ImageNet ResNet-50
checkpoints it would not: their trained ordering is uncorrelated with the
initial one and the depth trend has the opposite sign, so any width chosen
before training would be chosen against a profile the network will not
have. The controlled experiment of Section~\ref{sec:learned} says that
this is not the bottleneck block's doing, since under one CIFAR-10 recipe
the bottleneck network keeps as much of its initial ordering as the basic
one; what a width chosen at initialisation is worth therefore depends on
the training regime, not on the block, and the regime that produces the
ImageNet reordering is not identified here. The trajectories add that the
profile settles well before accuracy does in all four architectures
measured, and that the ImageNet-like reversal of the depth trend appears
in every CIFAR-10 network in its first epoch and is undone.

What a width criterion should target follows from the same numbers. A criterion that reads $k^*(0.95)$ as ``the width in use'' would remove directions the classifier depends on (Section~\ref{sec:projection}); the functional count is nearer $k^*(0.999)$.
A criterion must divide by the attainable rank, or it will report every
expansion layer as three-quarters idle. And whether the count can be raised
by regularisation is not a matter of pushing the kernel toward an isometry
alone: Section~\ref{sec:decompose} shows the trained kernel's high-gain
directions are aligned with the input's high-variance ones
($\rho_{\rm align} \approx +0.9$), so the
prediction for orthogonalisation is a rise of about $0.3$ of $r_{\max}$,
not a jump to the ceiling, and the penalties actually used realise half of
that rise, in the layers the decomposition names, because they leave the
kernel well short of an isometry. A width-selection method built on this
statistic is a separate piece of work; what this paper fixes is the
quantity it should optimise and the checks it must pass.

The same protocol measures depth as naturally as width, and we have used
it that way only where a question forced us to: the block-level profile
of Section~\ref{sec:blockout}, the depth recursion of the data factor in
Section~\ref{sec:decompose}, and the reversal of the depth trend under
training in Section~\ref{sec:learned}. How much effective width each
block \emph{adds}, and which blocks a trained network could do without
\cite{veit2016ensembles}, is a different measurement with its own
controls, and we leave it to a study of its own.

\subsection{What this does and does not say}

Everything here is a linear measure of the channel covariance at one
tensor: not the intrinsic dimension of a manifold, not the rank of the
network's Jacobian, and not the spatial rank of a feature map. A layer whose
covariance has high rank may still carry a low-dimensional curved manifold,
and the gap is not small (Section~\ref{sec:four}). Nor is any statement
here a statement that a network built with fewer channels would train to
the same accuracy. The measurement is of the trained artefact, not of the
optimisation that produced it.

Two further things the paper does not say. It does not say that the
published profiles were wrong: each is a correct measurement of the tensor,
estimator and denominator it chose, and the disagreement between them is
at least partly a disagreement about those choices rather than about the
networks. And it does not say what the shape of the profile is. The
pre-registered shape hypotheses were tested with a rule that had no power,
the paired replacements find level differences and not shape differences,
and the only shape statement we make, the reversal of the depth trend in
ResNet-50, is made on six recipes and five seeds and is labelled
exploratory.

\subsection{Limitations}
\label{sec:limits}
Six, in rough order of how much they should temper the conclusions.

\emph{Error bars exist for ResNet-50 and for family-level statements only.}
Six recipes and five seeds put a between-checkpoint spread beside every
ResNet-50 number. The other families have one to six trained checkpoints and three seeds, enough for the ordering statistics of
Table~\ref{tab:families} and not for any statement about one of their
layers; VGG-16's two checkpoints are with and without batch normalisation,
so its recipe agreement compares two architectures. The VGG-16 trajectory
is one seed; the controlled experiment and the penalty test have three
seeds per arm, enough for their registered tests and not for layer-level
statements.

\emph{The pooled estimator is sample-limited for wide layers, ours included.}
Under pooling $n$ is the image count, so no dataset smaller than $10^5$ images
can estimate a $2048$-channel layer at $n/C \ge 50$. Eleven of ResNet-50's
pooled rows fail our own gate, and we decline to compare estimators there. This
is a limit on the published pooled measurements as much as on ours.

\emph{Everything here is linear.} $k^*$, PR, effective rank and stable rank are
all functionals of the channel-covariance spectrum. None estimates the
dimension of a manifold, and the gap is not small: Ansuini \emph{et al.}
\cite{ansuini2019intrinsic} report a PC-based dimension of order $200$ and a
TwoNN intrinsic dimension of order $18$ for the same VGG-16 layer. A layer
whose covariance has high rank may still carry a low-dimensional curved
manifold.

\emph{Not a claim about prunability.} That the directions beyond $k^*(0.999)$ can be truncated at inference without a loss the test resolves (Section~\ref{sec:projection}) does not establish that a network built with fewer channels would train to the same accuracy. The measurement is of
the trained artefact, not of the optimisation that produced it, and the
literature on lottery tickets and on over-parameterised optimisation gives
ample reason to keep those separate.

\emph{Scope.} ImageNet classification, with detection backbones only in an ablation and a transformer only in one exploratory profile (Section~\ref{sec:vit}), without the initialisation control that carries the convolutional comparisons. The one other dataset, CIFAR-10, enters through the
validation gate, the trajectories, the controlled block experiment and the
penalty test, and the comparison between the two datasets
(Section~\ref{sec:learned}) confounds class count with head depth. The
controlled experiment shows that the block is not what makes the ImageNet
ResNet-50 recipes reorder their layers, but it varies the block at CIFAR-10
scale ($32 \times 32$ inputs, ten classes, 100 epochs) and so cannot say
whether the ImageNet effect is the dataset's or the recipes'; the
experiment that would, the same three architectures under one ImageNet
recipe with several seeds, is the one this study could not afford. The
pre-registered experiment that separates the two, varying head depth at
fixed class count, was not run, and the terminal collapse reported on
CIFAR-10 remains unexplained here. Arguments about the first convolutional
layer are additionally bounded by the dimension of the input patch and
confounded by the observation of Bell and Sejnowski \cite{bell1997ica} that
such layers learn filters resembling independent rather than principal
components.

\emph{The inverted-residual finding is about available checkpoints.} The MobileNetV3 checkpoints differ in pre-training data and training length as well as in optimiser, and the five same-dataset EfficientNet-B0 checkpoints, which agree only moderately, span three training frameworks. The controlled MobileNetV2 (Table~\ref{tab:round3}) shows that the block does have a profile under one recipe, so the weak agreement is the recipes'; which ingredient of the recipes is not determined.

% =========================================================================
\section{Conclusion}

We set out to reconcile two published depth profiles of how much of its
width a convolutional layer uses, . Three choices that the profile literature
makes silently each move the profile by more than the differences it
reports: normalising by nominal rather than attainable width caps every
expansion layer at the expansion ratio and misreads bottleneck networks as
underused, in all 14 networks that have such layers; estimating the covariance after global pooling lowers the value at 687 of 703 layers, and by more than the profile's own step in 18 of 19 networks; and reading the block output instead of
the convolution output raises it by the expansion ratio in 161 of 161
blocks. With those choices fixed and stated, the protocol reproduces the one published CIFAR-10 table it can be compared with ($r = 0.98$, two layers differing by $0.2$), and what training changes differs across the families measured: trained networks largely keep the ordering the architecture gave them and gain a level, the profile is in place before the fit is, and a controlled experiment registered in advance finds that the block type does not alter this; the ImageNet ResNet-50 checkpoints, which do reorder their layers, and the inverted-residual checkpoints, which do not agree with one another, are both the work of their training regimes rather than of their blocks. Measurement code, the eigenvalue
spectra of every layer, and the analysis plan with its deviation log are
released so that the next profile can be compared with these on stated
terms.

% =========================================================================
\section*{Reproducibility}
\label{sec:repro}
Measurement code, analysis scripts, the run manifests, and the raw
eigenvalue spectra of every layer are at
\url{https://github.com/layerspec/effective-width}, together with the
analysis plan and its deviation log under version control, which is the
evidence that the analysis was fixed before the ImageNet data were seen.
The measurement itself is packaged: \texttt{pip install layerspec} gives
\texttt{layerspec.profile(model, loader)} and \texttt{layerspec.decompose(model,
loader)}, which return the per-layer tables of Sections~\ref{sec:method}
and~\ref{sec:decompose} for any PyTorch network with convolutional layers.
\todo{repository is private until submission; make public and add the release tag here.}

\appendices
\section{Proofs}
\label{app:proofs}

\emph{Proposition~\ref{prop:ostrowski}.} From the singular value
decomposition $W = U S V^{\top}$ (thin, $S$ of size $r \times r$),
$P = U S U^{\top}$ and $W_{\circ} = U V^{\top}$ satisfy $P W_{\circ} = W$, so
$\Sigma_{\rm out} = W \Sigma_{\rm patch} W^{\top} = P \Sigma_{\circ} P$.
Both $\Sigma_{\rm out}$ and $\Sigma_{\circ}$ are supported on the column
space of $U$, on which $P$ is invertible with eigenvalues
$\sigma_1, \ldots, \sigma_r$. Restricted to that space, Ostrowski's theorem
\cite[Thm.~4.5.9]{horn2013matrix} for the congruence $P \Sigma_{\circ} P$
gives $\lambda_i(\Sigma_{\rm out}) = \theta_i \lambda_i(\Sigma_{\circ})$ with
$\lambda_{\min}(P^2) \le \theta_i \le \lambda_{\max}(P^2)$, i.e.\
$\theta_i \in [\sigma_{\min}^2, \sigma_{\max}^2]$. \qed

\emph{Corollary~\ref{cor:kstar}.} Let $F_{\rm out}(k)$ and $F_{\circ}(k)$
be the fractions of total variance in the top $k$ eigenvalues of the two
spectra. From $\sigma_{\min}^2 \lambda_i(\Sigma_{\circ}) \le
\lambda_i(\Sigma_{\rm out}) \le \sigma_{\max}^2 \lambda_i(\Sigma_{\circ})$,
\[
  F_{\rm out}(k) \;\ge\; \frac{F_{\circ}(k)}{F_{\circ}(k) + \kappa^2 \bigl(1 - F_{\circ}(k)\bigr)},
\]
since the top-$k$ sum is scaled by at least $\sigma_{\min}^2$ and the tail by at most $\sigma_{\max}^2$. The right-hand side is at least $\tau$ exactly when
$F_{\circ}(k) \ge \kappa^2 \tau / (1 - \tau + \kappa^2 \tau) = \tau'$. Hence
the smallest $k$ with $F_{\circ}(k) \ge \tau'$ satisfies
$F_{\rm out}(k) \ge \tau$, which is the upper bound. The same two inequalities read the other way give $F_{\rm out}(k) \le \kappa^2 F_{\circ}(k) / (\kappa^2 F_{\circ}(k) + 1 - F_{\circ}(k))$, which is below $\tau$ exactly when $F_{\circ}(k) < \tau''$; hence every $k$ below $k^*(\tau''; \Sigma_{\circ})$ has $F_{\rm out}(k) < \tau$, which is the lower bound. For $\kappa = 1$ the
two spectra are proportional and the counts coincide; if also
$C_{\rm out} \ge d$ then $W_{\circ}$ has orthonormal columns, so
$\Sigma_{\circ}$ has the nonzero spectrum of $\Sigma_{\rm patch}$ truncated to
$r_{\max} = d$ dimensions. \qed

\emph{Proposition~\ref{prop:gate}.} Lawley's first-order expansion is $\mathbb{E}\,l_i = \lambda_i + (\lambda_i/n)\sum_{j \ne i} \lambda_j/(\lambda_i - \lambda_j) + O(n^{-2})$, and the trace of the sample covariance is unbiased. Summing over $i \le k$, the terms with both indices at most $k$ cancel in pairs ($\lambda_i\lambda_j/(\lambda_i - \lambda_j)$ is antisymmetric), leaving the double sum over $i \le k < j$, each term of which is nonnegative. Under $\lambda_j \le \lambda_i/2$ for $j > k$, $\lambda_i/(\lambda_i - \lambda_j) \le 2$, so each term is at most $2\lambda_j$ and the sum is at most $2k \sum_{j > k}\lambda_j = 2k\,(1 - F(k))\operatorname{tr}\Sigma$. \qed

\emph{Proposition~\ref{prop:truncation}.} $\mathbb{E}\|r\|^2 = \operatorname{tr}\bigl((I - U_kU_k^{\top})\Sigma_\ell\bigr) = \sum_{i > k}\lambda_i$. The first-order change of the logits is $J_\ell r$, whose second moment is $\operatorname{tr}(J_\ell\,\mathbb{E}[rr^{\top}]\,J_\ell^{\top}) = \operatorname{tr}(J_\ell \Sigma_\ell^{\perp} J_\ell^{\top}) \le \|J_\ell\|_{\rm op}^2 \operatorname{tr}\Sigma_\ell^{\perp}$. For several layers the perturbations add and Minkowski's inequality bounds the root mean square of the sum by the sum of the root mean squares. \qed

\emph{Proposition~\ref{prop:compression}.} Let $Q$ ($r \times d$) have
orthonormal rows spanning the row space of $W$; then
$W_{\circ} = U Q$ for some $U$ with orthonormal columns, and
$\Sigma_{\circ} = U (Q \Sigma_{\rm patch} Q^{\top}) U^{\top}$ is unitarily similar
to the compression $Q \Sigma_{\rm patch} Q^{\top}$. The Cauchy interlacing
theorem \cite[Thm.~4.3.28]{horn2013matrix} gives the inequalities, with
equality of the nonzero spectra when $Q$ is square. Since $Q$ depends only
on the row space, so does $\Sigma_{\circ}$. \qed

\emph{Proposition~\ref{prop:depth}.} The patch vector is
$p = [a_1; \ldots; a_{k_h k_w}]$ with each $a_j$ almost surely in
$\mathcal{R} = \operatorname{range} \Sigma_{\rm act}^{(\ell)}$, so $p$ lies
almost surely in $\mathcal{R}^{k_h k_w}$, whose dimension is
$k_h k_w \operatorname{rank} \Sigma_{\rm act}^{(\ell)}$; the covariance of a
random vector supported on a subspace has rank at most the subspace's
dimension. The second inequality follows from
$\operatorname{rank}(W \Sigma W^{\top}) \le \min(\operatorname{rank} W,
\operatorname{rank} \Sigma)$ and $\operatorname{rank} W \le C_{\ell + 1}$. \qed

\subsection*{Limiting cases}

Each proposition has a regime in which it says nothing, and the regime is worth naming.

\emph{Proposition~\ref{prop:gate}, no spectral gap.} The expansion is termwise in $1/(\lambda_i - \lambda_j)$ and requires $\lambda_k > \lambda_{k+1}$; as the gap at $k$ closes the bound loses its content. The statistic does not: $\hat F(k)$ remains biased upward, but $k^*(\tau)$ is then determined only to within the number of eigenvalues clustered at the cut, and that is the residual seen in the convergence data, where the layers whose $k^*(0.999)$ at $n/C = 50$ has not reached its final value differ from it by one component.

\emph{Proposition~\ref{prop:truncation}, $\tau \to 1$ and $\tau \to 0$.} The bound vanishes linearly in $1 - \tau$ and is trivial at $\tau = 0$, where it returns the whole energy; between, it is a first-order statement, and for a ReLU network the second-order term is not controlled, so the proposition fixes the scale of the perturbation and not the accuracy. The multi-layer bound is a triangle inequality and is attained only when the per-layer perturbations of the logits are collinear.

\emph{Corollary~\ref{cor:kstar}, $\kappa \to \infty$.} As $\kappa$ grows $\tau' \to 1$ and $\tau'' \to 0$, so the two bounds become $1$ and $r$ and the interval is empty of information. The interval is informative only while $\kappa^2 (1 - \tau) \ll \tau$: at the median condition number of the trained kernels, $\kappa = 10.6$, and $\tau = 0.999$, the bounds are $k^*(0.90)$ and $k^*(0.99999)$ of the counterfactual spectrum, and at $\tau = 0.95$ they are $k^*(0.14)$ and $k^*(0.9995)$. This is why Section~\ref{sec:decompose} reports the bound's information content per layer rather than assuming it.

\emph{Proposition~\ref{prop:compression}, $r < d$.} When the kernel's row space is a proper subspace of the patch space the interlacing is strict in general; the upper bound is attained if and only if the row space contains the top-$r$ eigenvectors of $\Sigma_{\rm patch}$, the lower if and only if it is spanned by the bottom $r$. For $r < d$ the counterfactual therefore depends on \emph{which} $r$ directions of the input the kernel keeps, a choice that training makes and the polar factor does not undo; the propositions bound its effect by interlacing and do not determine it. The case $r = d$, where nothing is chosen, is the $1\!\times\!1$ expansion, $C_{\rm out} \ge d$.

\emph{Proposition~\ref{prop:depth}, saturation.} The bound $k_h k_w \operatorname{rank} \Sigma_{\rm act}^{(\ell)}$ exceeds $d$ as soon as the previous layer's post-activation covariance is full-rank, and it then says only $\operatorname{rank} \Sigma_{\rm patch}^{(\ell+1)} \le d$, which is Equation~\eqref{eq:rmax}; it binds only after a layer whose activation rank has collapsed, as in the CIFAR-10 tail of Section~\ref{sec:learned}.

\section{Claims, evidence and status}
\label{app:claims}

Table~\ref{tab:claims} lists every claim the paper makes, the evidence it
rests on, whether it was pre-registered, and where the numbers are.

\begin{table*}[t]
  \centering
  \caption{Every claim, its evidence, and its status. \emph{Pre-reg.}:
  the comparison was specified in the released analysis plan before the
  ImageNet data were collected; \emph{expl.}: arrived at after seeing the
  data. Counts are over layers unless stated. Every number is produced by
  \texttt{checkpoint\_analysis.py} from the released spectra.}
  \label{tab:claims}
  \scriptsize
  \begin{tabular}{p{4.3cm}p{5.9cm}lp{2.5cm}}
  \toprule
  Claim & Evidence & Status & Where \\
  \midrule
  The protocol reproduces the CIFAR-10 profile of Garg \emph{et al.} & 13 layers, $r = 0.98$, MAD $0.05$; two layers differ by $0.2$ & gate & Sec.~\ref{sec:repro-garg} \\
  The protocol agrees with Elmoznino and Bonner on ImageNet & 16 ResNet-50 blocks, $\rho = 0.997$, median $|\Delta \log| = 0.063$ & gate (expl.) & Sec.~\ref{sec:repro-garg} \\
  $6{,}400$ images suffice for the position-sampled statistic & six checkpoints, $\rho \ge 0.999$, max diff $0.016$ vs $50{,}000$ & expl. & Sec.~\ref{sec:samplesize} \\
  Normalising by attainable rank removes the bounded/unbounded split & 14/14 models: $p < 10^{-2}$ under $C$, $> 0.01$ under $r_{\max}$; max $k^*(0.999)/r_{\max} \le 1$ in 22/22 & expl. & Sec.~\ref{sec:rmax}, Table~\ref{tab:bounds} \\
  Trained expansion layers are nearly full-rank in their input & $k^*(0.999)/r_{\max}$ $0.94$ (ratio 4), $0.99$ (ratio 6) & expl. & Table~\ref{tab:bounds} \\
  Pooling lowers the measured width & 687/703 dense, 79/80 depthwise; drop exceeds the profile's step in 18/19 networks & pre-reg. (count), expl. (step) & Sec.~\ref{sec:estimator}, Table~\ref{tab:pooling} \\
  Pooling does not reorder layers & $\rho$(depth, pooled$-$sampled) n.s.\ in 3/3 & pre-reg. & Sec.~\ref{sec:estimator} \\
  Block outputs exceed the bound on their last convolution & 161/161 blocks, factor $\approx$ expansion ratio & expl. & Sec.~\ref{sec:blockout}, Fig.~\ref{fig:blockout} \\
  Each statistic gives a reproducible profile & $k^*$, PR, erank: recipe agreement $0.85$--$0.89$ & pre-reg. & Sec.~\ref{sec:metrics} \\
  Head-weighted statistics reorder layers; RankMe does not & PR vs ruler $\rho$ $0.41$ (VGG-16 $-0.52$); RankMe $0.94$--$0.99$; $\alpha$ $-0.66$ & expl. & Table~\ref{tab:estimators} \\
  The threshold changes the level, not the ordering & $\tau \in \{0.9, \ldots, 0.999\}$ on VGG-16; six recipes at $0.999$ & pre-reg. & Sec.~\ref{sec:threshold} \\
  Trained $\ne$ random (ResNet-50) & six recipes vs one draw, 45--50/53, $p \le 4 \times 10^{-8}$; level $0.23 \to 0.47$ vs step $0.09$ & expl. & Sec.~\ref{sec:learned} \\
  Plain/basic/dense families keep the architecture's ordering & T--R $0.61$--$0.75$ vs ceilings $0.79$--$0.98$, four families & expl. & Table~\ref{tab:families} \\
  Bottleneck families learn their ordering, as a gradient & T--R $-0.08$, $0.20$, $0.33$, $0.46$; within-stage $0.27$--$0.66$; ResNet-101 above ResNet-50 & expl. & Table~\ref{tab:families} \\
  Block type is the cause & H9.1 \emph{not supported}: basic T--R $0.55$ vs bottleneck $0.50$, $p = 0.2$; level and depth trend of ImageNet ResNet-50 also absent & pre-reg. (round 3) & Table~\ref{tab:round3} \\
  An inverted-residual network under one recipe has a profile & H9.2 supported: MobileNetV2 seeds T--T $0.99$--$1.00$ vs R--R $0.68$ & pre-reg. (round 3) & Table~\ref{tab:round3} \\
  Inverted-residual checkpoints agree weakly or not at all & T--T $-0.18$ (4 ckpts), $0.40$ (5 same-dataset ckpts) & expl. & Table~\ref{tab:families} \\
  The profile settles before accuracy does (CIFAR-10) & VGG-16 epoch 40 ($\rho$ vs final $0.98$, accuracy $84 \to 94\%$, one seed); basic/bottleneck/MobileNetV2 epochs 60/40/40 at $84$--$88\%$, three seeds & expl. & Fig.~\ref{fig:traj}, Table~\ref{tab:round3} \\
  $k^*(0.999)$ bounds the width used (conventional recipes) & 6/8 checkpoints unchanged (McNemar); RSB recipes lose $1.7$ & expl. & Table~\ref{tab:projection} \\
  $\Sigma_{\rm out} = W \Sigma_{\rm patch} W^{\top}$ holds in the data & recomputed vs measured within $0.008$, five checkpoints & check & Sec.~\ref{sec:decompose} \\
  Training moves the bottleneck from data to kernel & random: follows data ($\rho = 0.94$); trained: follows kernel ($0.8$--$0.97$ in 6/9) & expl. & Table~\ref{tab:decompose}, Fig.~\ref{fig:decompose} \\
  Orthogonalising a trained kernel would add $\approx 0.3\,r_{\max}$ & 401/412 layers, nine checkpoints; Proposition~\ref{prop:ostrowski} & prediction & Table~\ref{tab:decompose} \\
  Penalised networks reach the counterfactual & P9.3 \emph{not supported}: half the predicted gain (SO $0.50$, SRIP $0.24$--$0.43$); kernels not isometric (erank $0.80$--$0.85$) & pre-reg. (round 3) & Table~\ref{tab:ortho} \\
  SO gains follow the predicted layer order, SRIP's do not & $\rho$(gain, pred.) $0.72$, $0.91$ vs $-0.14$, $-0.19$ (P9.4) & pre-reg. (round 3) & Table~\ref{tab:ortho} \\
  Width gained is unrelated to accuracy & P9.5: 12 runs, $\rho = 0.15$, accuracy within $\pm 0.3$ points & pre-reg. (round 3) & Table~\ref{tab:ortho} \\
  Trained kernels suppress the input's principal directions ($\rho_{\rm align} < 0$) & P9.10 as registered: 0/48 & pre-reg., not supported & Sec.~\ref{sec:decompose} \\
  Trained kernels amplify the input's principal directions ($\rho_{\rm align} > 0$) & 43/48 at $p < 0.01$; ImageNet median $+0.91$; random 20/20 $\approx 0$; rises with training, 7/9 & post hoc (sign reversed) & Sec.~\ref{sec:decompose} \\
  Conv-output ruler widths retrain without loss & C-10: $-0.22$ at $34.5\%$ params; C-100: $-0.59$ at $87\%$ & pre-reg.; yes / no & Table~\ref{tab:a18} \\
  Post-activation ruler widths retrain without loss & C-10: $+0.07$ at $67\%$, all seeds above the conv-output arm (P9.17, P9.18) & pre-reg. (plan 9.8); yes & Table~\ref{tab:a18} \\
  $\tau = 0.95$ widths cannot be trained to & $-2.15$ (C-10), $-5.8$ (C-100) & pre-reg.; yes twice & Table~\ref{tab:a18} \\
  Ruler allocation beats a uniform cut & C-10: 8/9, one tie; C-100: 0/9 & pre-reg.; no twice & Sec.~\ref{sec:a18} \\
  Propositions~\ref{prop:ostrowski}--\ref{prop:depth} hold on the data & Ostrowski 464/465, corollary 465/465 & check & Sec.~\ref{sec:decompose} \\
  \bottomrule
  \end{tabular}
\end{table*}

\bibliographystyle{IEEEtran}
\bibliography{refs}

\end{document}
