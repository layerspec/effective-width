# 新論文：題目、大綱、預期成果（2026-09-16，作者要求；第一稿）

## 題目

主案：**Width Is Not a Hyperparameter: Allocating the Channels of a Convolutional Network from Its Covariance, Before and During Training**

備案：*How Wide Should Each Layer Be? Covariance-Guided Width Allocation Before and During Training*

一句話主張：一個卷積網路每層的寬度可以在訓練前從架構與資料的二階統計算出、在訓練中依無標籤的前向統計在層間調撥；
結果是同準確率下更少的算力，或同算力下更準；而且每一步都有可證的界。

## 三隻腳（審稿人會拿來量的）

| | 內容 | 狀態 |
|---|---|---|
| 新理論 | 寬度三段定律（架構界 r_max → 初始剖面由資料二階統計定 → 訓練加水準、機制是核與輸入對齊）＋控制器三命題（A 可移除寬度的一階界、B 放寬的變異上限、C 反向注水最佳性） | A/B/C 已推導；三段定律的第一、三段已有量測，第二段（先驗預測）待做 |
| 新架構 | 可調撥寬度的卷積層＋控制器：CSSP 摺入縮、未滿足變異初始化放、注水分配、由對齊到位觸發 | 縮已驗證（P1）；放、注水、訓練迴圈待做 |
| 可用、可比 | 同準確率省訓練與推論算力；對手 Yuan 2023、GradMax、Firefly、NORTH\*、DemP、AWN | CIFAR 零重訓 67% 參數持平已有；ImageNet 曲線待做 |

## 大綱（TPAMI 12 頁）

**I. Introduction**（1 頁）
寬度是唯一還靠手選或搜尋的架構量；現有成長／剪枝法要梯度、要排程、要第二次訓練。主張：寬度可算、可調撥、有界。
貢獻五條：(1) 三段定律與其量測；(2) 命題 A–C；(3) 控制器；(4) 零重訓摺入結果；(5) 一次訓練追平／ImageNet 曲線。

**II. Related Work**（1 頁）
成長法（Net2Net、Firefly、GradMax、Yuan 2023、TINY、SENN、SWE）、縮法（MorphNet、DemP、slimming、HRank）、學寬度（AWN）、
supernet（slimmable、AutoSlim）；寬度量測（Garg、E&B、Ansuini、NORTH\*）；理論（Saxe、Xiao 2018、Daneshmand、Feng、Han／Kim 界）。
每個只寫用到的結論。

**III. Effective Width from the Covariance Identity**（2.5 頁）
A. 恆等式 Σ_out = WΣ_patch W^T、r_max、量測協定（閘門、估計量、張量——現稿 §II 壓成一頁）。
B. 三段定律：架構界；初始剖面（先驗預測，對照 11 個家族的隨機剖面）；訓練加水準（對齊 ρ 0.9、epoch 7–15），
   plain／basic／dense 家族順序不變（T–R 0.6–0.75）；bottleneck 在 ImageNet 配方下重排（邊界）。
C. 命題 A（可移除寬度＝後激活共變異數的座標子集，CSSP 兩側界）、B（未滿足變異）、C（注水最佳性，無 τ）。

**IV. The Controller**（2 頁）
縮運算子（CSSP＋摺入下一層／分類器）、放運算子（未滿足變異方向初始化、下一層零行）、注水（守參數或 FLOPs）、
時程（訓練前先驗注水；對齊到位與剖面定時各讀一次）、殘差網路只調 block 內部、額外成本（幾次前向、無標籤）。

**V. Experiments**（4.5 頁）
A. 先驗剖面 vs 量到的隨機剖面（11 家族，ρ 對 R–R 天花板）。
B. 零重訓摺入：CIFAR-10／100 VGG-16、ImageNet ResNet-18／50——參數與 FLOPs 對準確率的曲線，對照結構化剪枝不微調的基線。
C. 一次訓練追平兩次訓練：VGG-16、ResNet-20 CIFAR-10／100，三臂（全寬、均勻、尺重訓）＋控制器（P2、P3）。
D. ImageNet ResNet-50 對 Yuan 2023／GradMax／Firefly／Net2Net：訓練 FLOPs vs top-1。
E. 資料複雜度曲線：類別數 10→1000（CIFAR 子集、ImageNet 子集），控制器省下的算力隨複雜度單調收斂（A24／A25 的定律變成用途）。
F. 消融：訊號（本文 vs 梯度範數 vs γ）、觸發時刻（統計量觸發 vs 固定排程）、ε 不敏感、只縮／只放。
G.（選配）ViT 的 MLP 寬度。

**VI. Discussion and Limitations**（0.5 頁）
代理目標與準確率只有上界；尾端變異可能是任務不需要的（CIFAR-100）；stage 寬度未調；bottleneck 重排的原因。寫一次，不道歉。

**VII. Conclusion**（0.25 頁）

補充：證明、量測協定細節（現稿的 §III 大半）、44 個 checkpoint 的表、主張表。

## 預期成果（已有的標「有」，目標標「要」）

| 結果 | 數字 | 狀態 |
|---|---|---|
| 零重訓摺入 VGG-16 CIFAR-10 | 67% 參數 93.82 vs 93.83；39% 93.34 | 有（P1） |
| 命題 A 的界 | 比值 1.02–2.84，遠在 (m+1) 內 | 有 |
| 尺寬度重訓（對照臂） | 67% 參數 93.84；均勻 93.20 | 有（A18） |
| 類別數定律 | 尺保留 0.88→0.99 隨 10→100 類單調 | 有（A24／A25） |
| 訓練 vs 初始化、對齊 | T–R 0.6–0.75／−0.08；ρ_align 0.9、epoch 7–15 | 有（現稿） |
| 先驗剖面預測 | 各家族 ρ ≥ 0.75（天花板 0.79–0.98） | 要（Mac，2–3 週） |
| 一次訓練追平（P2） | 守 67%／34.5% 參數 ≥ 尺臂 −0.3、> 均勻臂 | 要（Mac，各半天） |
| 多類不縮（P3） | CIFAR-100 控制器 = 全寬 ± 0.3 | 要（Mac） |
| 零重訓摺入 ImageNet ResNet-18／50 | k\*(0.99) 掉多少——未知；預期 ≤ 1 點於 ≥ 20% 參數減少 | 要（pod，US$5–10） |
| ImageNet ResNet-50 對 Yuan | ≤ 60% 訓練 FLOPs、≤ −0.3；或同 −0.8 下 ≤ 50% | 要（pod，US$100–300，餘額 60） |
| 複雜度曲線 | 10→1000 類，省下算力單調收斂到 0 | 要（CIFAR 免費；ImageNet 子集 US$20–40） |

## 風險（寫一次）

1. 先驗剖面預測若只到 ρ 0.5–0.6，第二段定律降為「順序大致可預測」，論文仍成立但理論腳變短。
2. ImageNet 上控制器若贏不了 Yuan 的 60%，「可比」那腳只剩 CIFAR 與零重訓摺入；那時題目改為「零重訓的寬度讀取與調撥」，仍是方法論文。
3. 錢：ImageNet 曲線是幾百美元；先做 CIFAR 與零重訓 ImageNet（便宜），有結果再決定加值。

## 算力需求估計（2026-09-16 深夜，作者問）

依據：A18 VGG-16 CIFAR 100 epoch 在 4090 約 30 分鐘（3 種子 × 2 預算「約 3 小時、< US$5」）、Mac MPS 慢 5–8 倍；
A18-ImageNet ResNet-18 3 臂 × 2 種子花 US$70–90（每跑約 12–15）；ResNet-50 90 epoch 約 ResNet-18 的 2.5–3 倍（每跑 35–45）。
RunPod 4090 約 US$0.35–0.5/h、A100 約 1.2–1.9/h。餘額 60.37。

| 項目 | 在哪 | 機器時間 | 錢 |
|---|---|---|---|
| 放運算子、注水、單元測試、P5、先驗剖面分析 | Mac | 開發數天；跑幾小時 | 0 |
| P2 一次訓練 VGG-16 CIFAR-10（3 種子 × 2 預算）＋ ResNet-20 對 Yuan 的 CIFAR 表（3 種子） | Mac | 9 跑 × 3–4 h ≈ 1.5 天（隊列過夜） | 0 |
| P3 CIFAR-100（3 種子） | Mac | 半天 | 0 |
| P4 放的方向 vs GradMax（6 跑）、消融訊號／觸發（9 跑） | Mac | 2–3 天 | 0 |
| 類別數曲線 CIFAR（4 個類別數 × 3 種子，控制器版） | Mac | 2 天 | 0 |
| 零重訓摺入 ImageNet ResNet-18／50（推論＋累積共變異數，`data/imagenet_val_6400` 已在本機；殘差版摺入要寫程式） | Mac | 數小時 | 0 |
| **Mac 小計** | | **約 1.5–2 週隊列時間**，可與寫作並行 | **0** |
| ImageNet 子集複雜度曲線（ResNet-18，100／10 類，3 臂 × 2 種子） | pod | 12 跑 × 1–2 h | 20–30 |
| ImageNet-1k ResNet-18 控制器（3 臂 × 2 種子；先做這個再做 R50） | pod | 6 跑 × 6–8 h | 80–90 |
| ImageNet-1k ResNet-50 控制器 vs Yuan 2023（全寬 1 跑＋控制器 2 種子，控制器約 60% 算力） | pod | 3 跑 × 12–24 h | 100–130 |
| A17 縮減版（basic vs bottleneck，2 種子）——邊界解釋，可選 | pod | 4 跑 | 120–150 |
| 資料抓取、磁碟、除錯餘裕 | pod | | 30–50 |
| **RunPod 小計** | | | **最低約 150（子集＋R18）；可信約 300–350（加 R50）；完整約 450–500（加 A17）** |

順序：Mac 全做完、零重訓 ImageNet 摺入有數字之後，再花 pod；先子集與 ResNet-18（餘額 60 夠子集加一種子），R50 看 R18 結果再決定。

## 運算清單（2026-09-16 午後，作者要求：要跑什麼、在哪跑、多少錢、做了沒）

| # | 運算 | 設備 | 錢（US$） | 狀態 |
|---|---|---|---|---|
| 1 | 44 個 checkpoint × 11 家族的剖面量測＋隨機初始化（3–5 種子） | Mac／早期 pod | 已花 | **完成**（現稿） |
| 2 | 10 個 checkpoint 的 Σ_patch 分解＋ 8 個家族隨機初始化的分解 | Mac | 0 | **完成**（隨機 7 個今天補） |
| 3 | A18 尺定寬重訓 CIFAR-10／100（5 臂 × 3 種子）、A23 ImageNet ResNet-18（3 臂 × 2 種子） | pod | 已花約 100 | **完成** |
| 4 | A24／A25 類別數與步數控制 | Mac | 0 | **完成** |
| 5 | 隨機 ViT-B/16 對照 | Mac | 0 | **完成** |
| 6 | P1 零重訓摺入：VGG-16 CIFAR-10 三種子、CIFAR-100 三種子、逐層表 | Mac | 0 | **完成** |
| 7 | 零重訓摺入 transformer：ViT-B/16、ConvNeXt-T、DeiT-S | Mac | 0 | **完成**（DeiT-S 的 ε 方案漏跑） |
| 8 | 放運算子、注水、單元測試（開發） | Mac | 0 | 待做，下個 session 起 |
| 9 | P2 一次訓練 VGG-16 CIFAR-10（3 種子 × 2 預算）＋ ResNet-20（3 種子） | Mac | 0 | 待做，約 1.5 天隊列 |
| 10 | P3 CIFAR-100 一次訓練（3 種子） | Mac | 0 | 待做，半天 |
| 11 | P4 放的方向 vs GradMax（6 跑）；消融訊號／觸發（9 跑） | Mac | 0 | 待做，2–3 天 |
| 12 | 類別數曲線 CIFAR 控制器版（4 × 3 種子） | Mac | 0 | 待做，2 天 |
| 13 | 先驗剖面預測（用 #2 的隨機分解，分析） | Mac | 0 | 待做，開發 2–3 週 |
| 14 | 零重訓摺入 ImageNet ResNet-18／50（殘差版摺入程式） | Mac | 0 | 待做，數小時 |
| 15 | DeiT-S ε 補跑；小 ViT 在 CIFAR 上的控制器（選配） | Mac | 0 | 待做，1–2 天 |
| 16 | D1 影像 bootstrap 信賴區間 | Mac | 0 | 待做，半天 |
| 17 | ImageNet 子集複雜度曲線（ResNet-18，100／10 類，3 臂 × 2 種子） | pod | 20–30 | 待做 |
| 18 | ImageNet-1k ResNet-18 控制器（3 臂 × 2 種子） | pod | 80–90 | 待做 |
| 19 | ImageNet-1k ResNet-50 對 Yuan 2023（全寬 1 跑＋控制器 2 種子） | pod | 100–130 | 待做，看 #18 結果 |
| 20 | A17 縮減版（basic vs bottleneck，2 種子；邊界解釋） | pod | 120–150 | 選配 |
| 21 | 資料、磁碟、除錯餘裕 | pod | 30–50 | — |

pod 合計：最低約 150（#17、#18、#21）、可信約 300–350（加 #19）、完整約 450–500（加 #20）；餘額 60.37。
Mac 部分（#8–#16）全部免費，約 2–3 週隊列時間，先做完再花 pod。

## 新舊版本對照與審稿意見回應（2026-09-16 午後，作者要求）

| 面向 | 舊版（12 頁量測論文） | 新版（寬度不該是超參數） |
|---|---|---|
| 題目 | 量測有效寬度的協定、陷阱與初始化對照 | 訓練前算出、訓練中調撥每層寬度，同準確率省算力 |
| 主張的性質 | 「三個量測選擇比已報告的效應大」（描述性） | 「寬度可算、可調撥、有界」（預測性＋方法） |
| 理論 | 一個恆等式、教科書級命題（閘門、截斷一階、Ostrowski） | 三段定律＋命題 A（可移除寬度＝座標子集、CSSP 兩側界）、B（未滿足變異上限）、C（注水最佳性，無 τ） |
| 方法／架構 | 無；「寬度選擇方法是另一篇」 | 可調撥寬度的卷積層＋控制器（CSSP 摺入縮、未滿足變異放、注水、逐層錯開觸發） |
| 用途 | 報告剖面時該說明的六件事 | 零重訓拿掉參數（CIFAR-10 三分之一不掉分）；一次訓練追平兩次；ImageNet 省訓練算力 |
| 因果 | 「不是 block」的推論（CIFAR 控制實驗）＋ 對齊的相關性 | 控制器本身是介入：改寬度看準確率；對齊當訊號被前瞻性檢驗 |
| 對手 | 兩篇量測文獻（Garg、E&B） | Yuan 2023、GradMax、Firefly、NORTH\*、DemP、AWN（有曲線可比） |
| 架構範圍 | CNN，ViT 一個 checkpoint 在補充 | CNN 訓練中調撥；transformer／ConvNeXt 零重訓摺入（運算子通用） |
| 預登記失敗清單 | 十三條列在限制段 | 舊假設不再是論文的問題；新登記 P1–P5 |
| 舊資料的去處 | 主體 | 證據章（三段定律的第一、三段）與補充 |
| 已在手的新證據 | — | P1：CIFAR-10 三種子 67% 參數 −0.04；CIFAR-100 不縮；ViT-B／ConvNeXt／ResNet 零重訓表；八家族隨機分解 |

| 審稿指控 | 舊版 | 新版 |
|---|---|---|
| 沒有新理論（R1-2、R2-MC2、M6） | 承認 | 命題 A–C 是新的（CSSP 用於寬度、注水判據）；三段定律待驗證第二段 |
| 沒有方法／架構（R1-2、R2-4） | 承認 | 控制器 |
| 沒有用處、對剪枝無指導（R1-5、R2-MC1／MC3） | 承認 | 零重訓摺入已是可用結果；訓練算力曲線待做 |
| 相關非因果、無介入（R2-MC2／MC6、M7） | 措辭 | 控制器＝介入 |
| CIFAR-100 尺輸均勻（R2-MC3） | 解釋 | 「多類不縮」是預測器的正確判斷，零重訓資料已證 |
| 只做 CNN（R1-1、R2-3.4） | 限制段一句 | transformer／ConvNeXt 零重訓表；訓練中調撥仍只 CNN |
| τ=0.999 無理論依據（R2-3.2） | 承認 | 注水不用 τ |
| 統計（R2-MC7、M3） | 未修 | 仍要做 D1（便宜） |
| 算力不足、ImageNet 不完整（R1-4） | 承認 | 未解；ImageNet 曲線是新版最大風險 |
| 預登記失敗＝無預測力（R1-3、R2-MC8） | 列清單 | 消失；新登記 |

接受率：舊版 25–30%；新版最好情況 50–55%、合理 35–45%、差的情況回到 25–30%（但多了零重訓摺入這個獨立賣點）。

## 價值主張（作者 2026-09-16 午後定調）

論文交付的是一個**調度器**：掛到通用網路上，決定每層寬度何時放、何時收、收多少。它的價值由三件事相乘：
(1) **對**——每個動作有證明（命題 A–C）；(2) **有效益**——速度、硬體資源或品質至少一項明顯改善，用數字說；
(3) **好推廣**——別人寫一份 Site 清單就能掛到自己的架構。論文因此要多量一個東西：**移植成本**（每個配接器的行數與時間），
與效益一起列表。第一版的限制寫一次：只調 block 內部（殘差流不動），效益在少類任務是參數與推論算力、在多類任務是訓練算力。

## 兩個層面的效益（作者 2026-09-17）

把資料當車流，調撥有兩種用法，論文兩種都要量：
1. **硬體節省層面**：預算小於全寬——同準確率下省參數、推論算力、訓練算力（P2 的 0.673／0.345 臂、ImageNet 對 Yuan）。
2. **資料利用層面**：硬體已定（車道就在那裡），總寬度守恆，只在層間調撥，讓資料被最有效地承載——注水的目標函數
   （最小化被丟棄的變異＝最大化通過的變異）正是這個意思；未滿足變異就是「沒被載到的車流」。實驗：從 A18 的均勻縮網路
   （0.339 全寬）出發、守恆參數只調撥，對均勻臂 93.20 與尺臂 93.55（`results/mac_queue_p2b.sh`，排在 P2 之後）。
   多任務車道分配（揭露書 6.7）也屬於這一層。
