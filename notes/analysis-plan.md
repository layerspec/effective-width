# 分析計畫（第一篇）

**寫作日期：2026-09-03，在看到任何資料之前。**

這份文件的用途是把「每一種可能的結果各代表什麼」預先訂死。第一篇論文的核心章節
（V-C，調和 Garg 與 Elmoznino & Bonner）在跑完之前內容是未知的，而這正是最容易
出現「拿到資料才編故事」的地方。預先寫下來，並用 git 追蹤之後的每一次修改，
是唯一能證明我們沒有事後挑選的方法。

**修改規則：** 這份計畫可以改，但改動必須是獨立的 commit，且在文末「偏離紀錄」
留下一行說明原因。看過資料之後才做的改動，一律標記為 exploratory。

---

## 1. 主要結果與主要比較

**主要結果量（primary outcome）**

> k\*(τ=0.95) / C，對相對深度作圖，僅限 conv 模組輸出、僅限 `n_over_C_ok=True` 的層。

只有這一個是 confirmatory。其餘一切——τ 的其他取值、activation 輸出、
participation ratio、effective rank、stable rank、冪律指數——都是 secondary 或
exploratory，論文中必須明確標示。

**為什麼是 0.95 而不是 Garg 的 0.999**：0.999 極度受譜尾影響，而譜尾正是取樣量
偏誤最先污染的地方。0.999 只在與 Garg Table 2 的對照中使用。

**主要比較**

> 在**同一批層**上，k\*/C 的形狀與 PR/C 的形狀是否相同。

形狀的操作型定義（避免「看起來像駝峰」這種說法）：

- `rho_all` = 指標與深度序位的 Spearman 相關（全部層）
- `rho_late` = 同上，但只取後 1/3 的層

| rho_all | rho_late | 判定 |
|---|---|---|
| > 0.5 | > 0 | 單調上升 |
| 任意 | < −0.5 | 駝峰（後段塌縮） |
| 其他 | 其他 | 無明確形狀，據實報告 |

---

## 2. 四個候選解釋，以及各自在資料上的特徵

兩篇既有結果的矛盾，有四個可能來源。它們在我們的資料上留下**不同且可分辨**的
特徵，這是本研究能作出裁決的原因。

### H1 · 指標定義

PR 由譜的頭部主導（(ΣL)²/ΣL²），k\* 則要數到尾部累積達 τ。譜的頭部集中而尾部拉長
時，兩者會朝相反方向走。

**特徵**：在完全相同的層上，k\*/C 呈駝峰而 PR/C 單調上升。
**若成立**：矛盾純粹是指標造成的，兩篇都沒錯。這是最乾淨的結果，但也最平淡。

### H2 · 空間池化 ★ 需要修改管線

Elmoznino & Bonner 是在**全域池化之後**計算共變異數；我們抽樣空間位置。全域池化
把空間變異整個平均掉，得到的共變異數是不同的物件。這在方法上是很大的差異，而且
沒有人並排比較過。

**特徵**：同一批層上同時算「位置抽樣」與「全域池化」兩種共變異數。若池化版本單調
上升而位置抽樣版本呈駝峰，池化就是解釋。

**行動**：管線必須加上全域池化的累積器。**這是寫這份計畫才發現的缺口。**

### H3 · 類別數與終端塌縮

Neural collapse 把倒數第二層的有效秩釘在 C_class − 1。CIFAR-10 是 9，
ImageNet-1k 是 999。Garg 的深層塌到 0.07 是在 CIFAR-10 上量的——512 通道的層
本來就只需要約 9 維。

**特徵**：我們同時有兩個 regime——Garg 重現關卡跑的是 CIFAR-10，主實驗跑的是
ImageNet。若深端在 CIFAR 上塌縮而在 ImageNet 上不塌，類別數就是解釋。
**這是設計出來的對照，不是巧合。**

### H4 · 量測範圍截斷

Elmoznino & Bonner 量的模型多半在分類頭之前就截斷。若他們的層集合根本沒進入塌縮
區，那他們看到單調上升是正確的，**兩篇之間並不存在矛盾**。

**特徵**：我們的曲線一路量到最後一個 conv 層。若上升持續到最後一個 conv 層、
只有在分類頭才下降，H4 成立。

### H5 · 取樣量

n 接近或小於 C 時，樣本共變異數在構造上秩虧損，會製造出低秩假象。

**特徵**：收斂控制圖。若某層的 k\*/C 在最大可得的 n/C 處仍未壓平，該層不予報告。
**若既有文獻的深層曲線有一部分是這個假象，那本身就是發現**，而且是這篇最強的
可能結果。

### H5b · 既有結果本身的取樣量 ★ 新增

Elmoznino & Bonner 用 10,000 張影像、全域池化後計算共變異數，也就是
**n = 影像數**。對 ResNet-50 最後的 conv 層（C = 2048），n/C ≈ 4.9。
這遠低於任何合理的門檻。

**特徵**：我們用全域池化估計量，分別在 n = 10,000 與 n = 50,000 張影像下計算
PR/C。若 PR/C 隨樣本數明顯移動，他們觀察到的單調上升就有一部分是取樣量效應。

**注意**：PR 由譜的頭部主導，因此比 k\* 耐取樣不足，這個檢定不必然會顯著。
但它便宜（同一趟就能算），而且若顯著，是本篇最強的結果。

**同時要誠實記錄**：我們自己的全域池化估計量受同一個限制——50,000 張影像對
C = 2048 只有 n/C ≈ 24，低於我們自訂的 50 門檻。這一點必須寫進論文，
不能只用來批評別人。

### 預先聲明

以上五者**可以並存**。論文的任務是報告每一個的證據強度，而不是挑一個宣布勝出。
若證據指向多因，就照實寫多因。

---

## 3. 判定關卡

依序通過，任何一關失敗都不得進入下一關。

**關卡 1 · Garg 重現**（`scripts/reproduce_garg.py`）

| 結果 | 動作 |
|---|---|
| r > 0.9 且 MAD < 0.10 | 通過，進行 ImageNet |
| 0.7 < r < 0.9 | 調查不合的層，記錄原因後再決定 |
| r < 0.7 | **停**。問題在我們的管線或訓練，不在文獻 |

**關卡 2 · 取樣量**

任何 `n_over_C_ok = False` 的列不得出現在論文的主要圖表中。若某架構有超過 1/3 的
conv 層未通過，該架構整個退出主要分析，並在限制一節說明。

**關卡 3 · 訓練 vs 隨機初始化**

若在所有架構上訓練與隨機初始化的曲線都無法區分（逐層差異的 95% 信賴區間涵蓋 0），
**論文的主張要改寫**：剖面是架構與輸入統計的性質，不是學習的產物。這會是重要的
負面發現，但它會削弱第二篇（寬度判準）的動機，必須誠實寫出來。

---

## 4. 預先指定的圖表

| 編號 | 內容 | 地位 |
|---|---|---|
| Fig 1 | k\*/C vs 深度，逐架構，訓練 vs 隨機 | primary |
| Fig 2 | k\*/C vs PR/C vs eff.rank/C，同一批層 | primary（H1 的裁決） |
| Fig 3 | 收斂控制：指標 vs n/C | primary（授權其餘一切） |
| Fig 4 | τ 敏感度 | secondary |
| Fig 5 | 位置抽樣 vs 全域池化 | primary（H2 的裁決）**待實作** |
| Fig 6 | CIFAR vs ImageNet 深端對照 | primary（H3 的裁決） |
| Table 1 | 與 Garg Table 2 的逐層對照 | 關卡 1 |
| Table 2 | rho_all / rho_late，逐架構逐指標 | primary |

---

## 5. 不做的事

- **不做 Marchenko–Pastur 門檻。** CNN 激活譜是冪律、α ≪ 1，沒有 bulk edge。
  這點在論文中要正面說明，因為讀者會問。
- **不宣稱流形維度。** 這裡全部是線性測度。TwoNN 那類估計不在本篇範圍。
- **不從單一模型的權重推論。** 每個架構只有一組公開預訓練權重，這是限制，
  不是可以忽略的細節——寫進 limitations。
- **不在看到結果之後更改主要結果量。** 若真的必須改，記在偏離紀錄裡並標記
  exploratory。

---

---

## 7. 第一輪結果（2026-09-03）與其後的探索性發現

**預先登記的部分**（判定由 `layerspec/analyse.py` 計算，不是目測）：

- **H2 成立，而且是本篇的核心結果。** VGG-16 在同一批層上，位置抽樣給駝峰、
  全域池化給單調上升，而池化那組 13/13 全數通過取樣量門檻。ResNet-18 同樣
  （20/20 通過）。**Garg 與 Elmoznino & Bonner 的矛盾來自估計量，不是指標。**
- **H1 不成立。** 六個模型裡有四個 k\* 與 PR 給出相同形狀。
- **H5 良好。** 除 VGG 的 1/13 之外，所有層在最大 n/C 處均已壓平。
- **訓練 vs 隨機初始化：可區分。** ConvNeXt 平均差 +0.085（17/22 層訓練較高），
  ResNet-50 +0.108（44/53）。剖面不是純架構性質，論文的框架維持不變。

**Garg 對照（τ=0.999，通道數逐層完全對齊）：**

- 前 8 層 **r = 0.969，MAD = 0.057** — 跨資料集重現，管線驗證通過
- 後 5 層分岔 +0.42 到 +0.90：Garg 塌到 0.07–0.08，我們維持 0.95–0.97
- **終端塌縮在 ImageNet 上沒有發生**

該分岔有兩個混淆的解釋，本輪無法分辨：
(a) 類別數（CIFAR-10 的 neural collapse 釘在 9；ImageNet 是 999）；
(b) 分類頭架構（Garg 的 CIFAR 版小頭讓 features.40 實質為倒數第二層；
ImageNet 版後面還有 4096-4096-1000）。

### 探索性發現（**非**預先登記，論文中必須標示）

**ResNet-50 的寬層只用了約一成寬度。** 同一個 bottleneck 內：
`layer4.2.conv1/conv2`（C=512）在 τ=0.999 為 0.941／0.945，
而 `layer4.2.conv3`（C=2048）為 0.233。按寬度分組的中位數 k\*/C（τ=0.95）：

    resnet50   64:0.23  128:0.34  256:0.40  512:0.37  1024:0.11  2048:0.13

這也解釋了 ResNet 為何判定「無明確形狀」——剖面在寬層與窄層之間震盪，
depth-ordered 的相關會被洗掉。**對 ResNet 而言深度是錯的 x 軸，該用 C。**

注意 VGG 上寬度與深度共變（ρ(C)=+0.755、ρ(depth)=+0.813），無法分離。

## 8. 偏離紀錄

每一次偏離這份計畫，在此加一行：日期、改了什麼、為什麼、是否為看過資料之後。

| 日期 | 改動 | 原因 | 看過資料? |
|---|---|---|---|
| 2026-09-03 | 建立本文件 | — | 否 |
| 2026-09-03 | 新增 H5b；管線加入全域池化共變異數 | 撰寫本計畫時發現 Elmoznino & Bonner 用的是池化估計量，與我們的位置抽樣不同，且其 n = 影像數導致深層嚴重取樣不足 | 否 |
| 2026-09-03 | 修正 probe 的鍵：加入模組在單次 forward 中的呼叫序號 | ResNet block 重複使用同一個 nn.ReLU，原本以模組名稱為鍵會把一個 block 內 多個激活位置合併成一個共變異數。ResNet-50 因通道數不同而崩潰，ResNet-18 因通道數相同而**安靜地**混合了兩個不同分布。conv 列不受影響，act 列在此 之前的結果全部作廢 | 否 |
| 2026-09-03 | H1／H2 判定由「任一模型不同即成立」改為多數決 | 原邏輯讓一個隨機初始化模型的例外壓過五個一致的模型，把 H1 誤報為 SUPPORTED。 這是報告程式的瑕疵，非資料問題 | 是（但修的是判定規則，不是主張） |
| 2026-09-03 | probe 加入 nn.Linear 層 | 為了檢驗終端塌縮是否為 neural collapse；但 4096 單元的分類層在 5 萬張影像下只有 n/C ≈ 12，**過不了自訂的 50 門檻**，且取樣不足的偏誤方向恰好與待測效應同向，因此此路無法作為乾淨檢定 | 是 |
| 2026-09-03 | 新增探索性發現：ResNet-50 寬層的 k\*/C 遠低於窄層 | 看資料時發現，非預先登記。論文中須標示為 exploratory | **是** |
